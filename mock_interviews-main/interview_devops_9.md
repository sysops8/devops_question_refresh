<div align="center">
<a href="https://devops-factory.com" >
  <img src="https://devops-factory.com/images/logogit.png" width="250" alt="DevOps Factory">
</a>
</div>
<div align="center">
    <a style="vertical-align: middle;display: inline-block;">
      <img align="center" src="https://readme-typing-svg.demolab.com?font=Fira+Code&size=14&duration=2000&pause=&color=3DD10D&center=true&vCenter=true&multiline=true&width=435&lines=%D0%A1%D0%B4%D0%B5%D0%BB%D0%B0%D0%BD%D0%BE+%D0%B0%D0%BA%D1%82%D0%B8%D0%B2%D0%B8%D1%81%D1%82%D0%BE%D0%BC+%D0%B8%D0%B7+%D0%BA%D0%BB%D1%83%D0%B1%D0%B0+%D0%BF%D1%80%D0%B8+%D1%84%D0%B0%D0%B1%D1%80%D0%B8%D0%BA%D0%B5;By+Dmitriy+Shmakov" alt="" />
    </a>  
  </a>
</div>

<div align="center">

<a href="https://devops-factory.com">
    <img src="https://img.shields.io/badge/DevOps%20Factory-green?style=for-the-badge" alt=""/>
</div>

<div align="center">
     <a href="=https://www.youtube.com/@devopsfactory">
    <img src="https://img.shields.io/badge/YouTube-red?style=for-the-badge" alt=""/>
   <a href="=https://t.me/@devopsfactory">
    <img src="https://img.shields.io/badge/Telegram-blue?style=for-the-badge" alt=""/>
</div>

# DevOps Interview Knowledge Base

Разбор **[мок собеса №9](https://youtu.be/5-MVBKH77tE)** в формате "Вопрос — Ответ" для подготовки к собеседованиям на позицию DevOps.

# 📌 Содержание

- [1. Load Balancing](#1-load-balancing)
- [2. Monitoring](#2-monitoring)
- [3. IaC, Terraform](#3-iac-terraform)
- [4. AWS](#4-aws)
- [5. Linux, Docker](#5-linux-docker)

---

# 1. Load Balancing

## ❓ 1. Чем Application balancer отличается от Network balancer?

**[📺 Видео-вопрос (10:59)](https://youtu.be/5-MVBKH77tE?t=659)**

<details><summary><strong>Показать ответ</strong></summary><p>

> Рассмотрим основные различия между **Application Load Balancer (ALB)** и **Network Load Balancer (NLB)**, которые используются в AWS. Эти два типа балансировщиков нагрузки имеют разные особенности, которые подходят для различных типов приложений.

## 1. **Уровень модели OSI** 🧑‍💻

- **ALB** работает на **7-м уровне модели OSI (прикладном уровне)**, что позволяет анализировать HTTP/HTTPS трафик и принимать решения на основе таких данных, как:
  - URL
  - Заголовки HTTP
  - Cookies
- **NLB** работает на **4-м уровне модели OSI (транспортном уровне)** и принимает решения о маршрутизации на основе **IP-адресов** и **портов**.

## 2. **Протоколы и типы трафика** 🌐

- **ALB** поддерживает **HTTP** и **HTTPS** трафик. Отлично подходит для веб-приложений.
- **NLB** поддерживает **TCP** и **UDP** трафик, что делает его идеальным для любых приложений с низкой задержкой, например:
  - Базы данных
  - Игровые серверы
  - VOIP приложения

## 3. **Производительность** 🚀

- **ALB** ориентирован на обработку веб-запросов и предоставляет дополнительные возможности, такие как поддержка веб-сокетов и более сложная маршрутизация.
- **NLB** разработан для **высокой производительности**, может обрабатывать миллионы запросов в секунду с минимальной задержкой.

## 4. **Маршрутизация трафика** 🔄

- **ALB** поддерживает сложную маршрутизацию, включая маршрутизацию по:
  - URL
  - Путям запроса
  - Хосту
  - И другие критерии
- **NLB** не поддерживает такую сложную маршрутизацию, он направляет трафик исключительно по **IP-адресу** и **порту**.

## 5. **Использование** 💻

- **ALB** идеально подходит для:
  - Веб-приложений
  - REST API
  - Микросервисной архитектуры
- **NLB** лучше использовать для:
  - Высокопроизводительных приложений
  - Сценариев с минимальной задержкой, например:
    - Базы данных
    - DNS-серверы
    - Службы на TCP/UDP

## 6. **Сессии и состояние** 🛠️

- **ALB** поддерживает **состояние сессии (sticky sessions)** для сохранения состояния между запросами.
- **NLB** не поддерживает состояние сессии, так как работает на более низком уровне.

## **Вывод** 🔍

- **ALB**: Используется для веб-приложений и анализа HTTP-трафика. Идеально подходит для **API** и **микросервисов**.
- **NLB**: Предпочтителен для **высокопроизводительных приложений** с минимальной задержкой, таких как **TCP/UDP приложения**.

### Выбор зависит от ваших требований:

- Если вам нужна гибкая маршрутизация для веб-приложений — выбирайте **ALB**.
- Если критична **низкая задержка** и обработка больших объемов трафика — выбирайте **NLB**.

</p></details>

---

# 2. Monitoring

## ❓ 1. Какой инструмент ты бы выбрал для мониторинга инфраструктуры нового проекта?

**[📺 Видео-вопрос (12:54)](https://youtu.be/5-MVBKH77tE?t=774)**

<details><summary><strong>Показать ответ</strong></summary><p>

> Для мониторинга инфраструктуры нового проекта идеально подходит связка **Prometheus** и **Grafana**. Это решение идеально подходит для микросервисов, контейнеризованных приложений и облачных платформ.

## 1. **Prometheus** 🔍

**Prometheus** — это система мониторинга, которая фокусируется на сборе метрик и идеально подходит для работы с динамическими средами, такими как микросервисы и контейнеры.

### Преимущества **Prometheus**:

- **Сбор метрик** 📊: Простой сбор метрик с помощью экспортеров, которые интегрируются с сервисами и приложениями.
- **Хранение данных** 💾: Использует свою систему хранения данных, оптимизированную для временных рядов.
- **Запросы с использованием PromQL** 🧮: Язык запросов PromQL позволяет гибко извлекать и агрегировать метрики.
- **Автодискавери** 🔄: Поддержка автоматического обнаружения новых сервисов в облачной инфраструктуре.

### Как это работает:

- **Экспортеры** собирают данные с приложений и сервисов, и Prometheus извлекает эти метрики.
- **Метрики** могут быть связаны с состоянием системы, производительностью и ошибками.
- **Алерты**: Настройка алертов для уведомлений о проблемах в системе.

## 2. **Grafana** 📊

**Grafana** — это платформа для визуализации данных, которая используется для отображения метрик, собранных Prometheus.

### Преимущества **Grafana**:

- **Визуализация данных** 🌈: Дашборды для интерактивного отображения метрик в виде графиков и диаграмм.
- **Поддержка множества источников данных** 🔗: Помимо Prometheus, Grafana интегрируется с различными источниками, например, Elasticsearch и InfluxDB.
- **Интерактивные дашборды** 🖱️: Возможность фильтрации данных по различным критериям.
- **Алерты** 🚨: Настройка уведомлений по меткам и событиям.

### Как это работает:

- **Grafana** подключается к **Prometheus** как источник данных и строит визуализации на основе метрик.
- **Дашборды** могут быть динамическими, обновляясь в реальном времени для мониторинга текущего состояния системы.

## Почему стоит выбрать связку **Prometheus + Grafana**? 🤔

### 1. **Масштабируемость** 📈

Идеально подходит для крупных проектов и облачных систем. Простой в настройке для микросервисов и контейнеров, таких как **Docker** и **Kubernetes**.

### 2. **Гибкость** 🔄

Позволяет собирать любые метрики и адаптировать систему под требования инфраструктуры. **Grafana** предоставляет широкие возможности для визуализации данных.

### 3. **Интеграция с современными системами** 🌐

**Prometheus** и **Grafana** легко интегрируются с облачными сервисами и контейнерами, что делает их идеальными для новых проектов, особенно в динамических средах.

### 4. **Простота настройки** ⚙️

Система настройки **Prometheus** и **Grafana** проста и интуитивно понятна, особенно в экосистемах **Kubernetes** и **Docker**.

## Пример использования 📋

Предположим, у вас есть **микросервисная архитектура** на базе **Kubernetes**. Вы хотите мониторить состояние сервисов и общую нагрузку на систему:

- **Prometheus** собирает метрики о **CPU**, **памяти**, **ошибках** и **запросах**.
- **Grafana** отображает данные на интерактивных **дашбордах**, чтобы команда могла быстро реагировать на изменения.

## Итоги 🏁

Система мониторинга на базе **Prometheus** и **Grafana** — это идеальный выбор для нового проекта. Это решение гибкое, масштабируемое и легкое в настройке, что делает его оптимальным для динамичных и облачных инфраструктур.

</p></details>

---

## ❓ 2. Почему Prometheus может быть хуже, чем CloudWatch или LogicMonitor для сбора метрик по загрузке сервисов, состоянию контейнеров, подов, нагрузке воркер-нод и других показателей? И можем ли мы обойтись только CloudWatch, если требуются такие метрики? Что проекты будет более выгодно с точки зрения финансов?

**[📺 Видео-вопрос (15:56)](https://youtu.be/5-MVBKH77tE?t=956)**

<details><summary><strong>Показать ответ</strong></summary><p>

> **Prometheus**, **CloudWatch** и **LogicMonitor** — все эти инструменты предназначены для мониторинга и сбора метрик, но они сильно различаются по архитектуре, функциональности и стоимости. Давайте разберемся, почему **Prometheus** может быть хуже, чем **CloudWatch** или **LogicMonitor** в некоторых случаях, а также, что будет более выгодно с точки зрения финансов.

## 1. **Преимущества и недостатки Prometheus по сравнению с CloudWatch и LogicMonitor**

### **Prometheus** 📊

**Prometheus** — это open-source система мониторинга, ориентированная на контейнеры и микросервисные архитектуры, с сильной поддержкой интеграции в **Kubernetes** и **Docker**.

#### **Преимущества:**

- **Гибкость**: Prometheus дает полную свободу в настройке метрик, алертов и визуализаций.
- **Сильная поддержка Kubernetes**: Прекрасно подходит для мониторинга контейнеров и подов в Kubernetes.
- **Хорошая производительность**: Prometheus хранит метрики локально в временных рядах и оптимизирован для сбора метрик от множества источников.
- **Open-source**: Нет затрат на лицензии.

#### **Недостатки:**

- **Не готов к масштабированию**: По умолчанию Prometheus не предназначен для масштабируемого хранения метрик на больших объемах данных.
- **Требуется настройка и обслуживание**: Для мониторинга и сбора данных потребуется настройка, поддержка серверов, хранилищ и алертов.
- **Отсутствие долгосрочного хранения данных**: Требуется настройка для долгосрочного хранения данных (например, через внешние решения).
- **Управление в распределенных системах**: Проблемы с управлением метками, шардированием данных и синхронизацией.

### **CloudWatch (AWS)** ☁️

**CloudWatch** — это облачный сервис от AWS, который предоставляет метрики, логи и алерты для сервисов AWS и других интеграций.

#### **Преимущества:**

- **Интеграция с AWS**: CloudWatch идеально подходит для мониторинга сервисов и инстансов в AWS (EC2, RDS, Lambda и т. д.), включая метрики и логи.
- **Легкость в настройке**: Нет необходимости в установке или управлении сервисами. Все работает "из коробки" для сервисов AWS.
- **Долгосрочное хранение**: CloudWatch позволяет хранить метрики на длительный срок.
- **Масштабируемость и управление**: AWS автоматически масштабирует CloudWatch и предоставляет поддержку, что позволяет масштабировать мониторинг без дополнительных усилий.
- **Платформа как услуга (SaaS)**: Нет необходимости заботиться о серверах и инфраструктуре.

#### **Недостатки:**

- **Стоимость**: CloudWatch может стать дорогим, особенно для больших объемов данных и долгосрочного хранения метрик.
- **Меньше гибкости**: CloudWatch предоставляет меньше возможностей для кастомизации, чем Prometheus.
- **Ограниченная поддержка не-AWS сервисов**: Интеграции с не-AWS сервисами могут потребовать дополнительной настройки.

### **LogicMonitor** 📈

**LogicMonitor** — это облачная платформа для мониторинга, которая предлагает решение для сбора метрик с разнообразных устройств, сервисов и облаков.

#### **Преимущества:**

- **Поддержка множества технологий**: LogicMonitor поддерживает разнообразные сервисы (например, сети, серверы, базы данных, контейнеры, облачные сервисы).
- **Автоматическая настройка**: LogicMonitor автоматически обнаруживает устройства и конфигурирует мониторинг, что облегчает настройку.
- **Обширная поддержка API и интеграций**: Множество интеграций с другими сервисами и инструментами.
- **Облачная платформа с возможностью масштабирования**: Предоставляет облачные ресурсы с минимальными усилиями по масштабированию.

#### **Недостатки:**

- **Стоимость**: LogicMonitor — это коммерческое решение, и его стоимость может быть высокой в зависимости от объема мониторинга.
- **Некоторая сложность в настройке**: Несмотря на автоматическое обнаружение, для сложных и кастомизированных решений потребуется дополнительная настройка.

## 2. **Можем ли мы обойтись только CloudWatch?** 💭

**CloudWatch** — это мощный инструмент, особенно если ваш проект активно использует сервисы AWS. Он предоставляет метрики и логи для всех сервисов AWS, что делает его отличным выбором для проектов в облаке, которые в основном используют **EC2**, **Lambda**, **RDS**, **ECS**, **EKS** и другие сервисы AWS.

Однако для **гибкого мониторинга контейнеров** (особенно в **Kubernetes**) или при **многокластерной инфраструктуре** может быть более эффективен **Prometheus**, так как он имеет сильную поддержку для таких сценариев и более гибок в настройке.

Если ваш проект активно использует **AWS** и вы не планируете мониторить внешние системы (кроме AWS), **CloudWatch** может быть достаточен для сбора метрик и логов. Однако для более сложных мониторинговых сценариев, кастомизации и работы с микросервисами можно добавить **Prometheus** или **LogicMonitor** для лучшей гибкости.

## 3. **Что будет более выгодно с точки зрения финансов?** 💰

- **CloudWatch**: Обычно является более дорогим вариантом, так как стоит на основе объема собранных метрик и времени хранения. Если вы храните метрики в течение длительного времени или собираете большое количество данных, это может привести к значительным расходам.
- **Prometheus**: **Open-source**, но требует настройки и инфраструктуры для масштабирования. Если вы уже используете Kubernetes или контейнеры, Prometheus может быть выгодным решением, особенно если у вас нет ограничений по бюджету на инфраструктуру.
- **LogicMonitor**: Это платное решение с подпиской. Оно может быть дороже, чем использование Prometheus, но легче в настройке и с широкими возможностями для мониторинга всех типов ресурсов, включая облачные и физические системы.

### Рекомендации:

- Для **проектов на AWS**: **CloudWatch** — лучший выбор с точки зрения интеграции, но с возможностью увеличения стоимости.
- Для **микросервисных и контейнерных архитектур**: **Prometheus** — лучший выбор по гибкости и стоимости, особенно если можно настроить инфраструктуру для масштабирования.
- Для **широких и гибких решений** с поддержкой всех типов ресурсов: **LogicMonitor** может быть удобным, но более дорогим вариантом.

## **Резюме:**

- **CloudWatch** — для проектов, использующих **AWS**, и простоты в настройке, но возможны высокие расходы.
- **Prometheus** — для проектов с **Kubernetes** и контейнерами, где важна гибкость и контроль, но требует настройки и масштабирования.
- **LogicMonitor** — для более универсальных решений с широким спектром интеграций, но с высокой стоимостью.

</p></details>

---

## ❓ 3. Как в CloudWatch настроить типичный алерт? Например, если у меня есть метрика о загрузке ноды, как настроить так, чтобы уведомление о превышении порога отправлялось в Slack или в другое место?

**[📺 Видео-вопрос (19:35)](https://youtu.be/5-MVBKH77tE?t=1175)**

<details><summary><strong>Показать ответ</strong></summary><p>

> В **AWS CloudWatch** вы можете настроить **alert** (или **оповещение**) для метрики, например, **загрузки CPU** на EC2 инстансе, и отправить уведомления в **Slack** через **SNS (Simple Notification Service)**.

## Шаги для настройки Typical Alert в CloudWatch:

### 1. **Создание метрики в CloudWatch** 📊

Если у вас уже есть метрика, например, **CPU Utilization** для вашей EC2-инстанса, то переходите ко второму шагу. Если метрика еще не создана:

1. Перейдите в **CloudWatch** в AWS Management Console.
2. Перейдите в раздел **Metrics** и выберите нужную метрику (например, **CPU Utilization** для вашего EC2).

### 2. **Создание CloudWatch Alarm** ⚙️

1. Перейдите в **CloudWatch**.
2. В меню слева выберите **Alarms**.
3. Нажмите **Create alarm**.
4. Введите имя метрики, например, **CPU Utilization**.
5. Установите **порог** (например, если нагрузка на CPU превышает 80% в течение 5 минут).
6. Выберите **Statistic** и **Period** (например, **Average** и **5 минут**).
7. Установите **Evaluation period** на 2 периода, чтобы alarm сработал только после 10 минут превышения порога.

### Пример конфигурации для метрики **CPU Utilization**:

- **Threshold**: 80% CPU utilization.
- **Period**: 5 минут.
- **Evaluation period**: 2.
- **Comparison**: Greater than threshold.

### 3. **Настройка действия (SNS Topic)** 📧

После того как вы создали метрику и пороговое значение, вам нужно настроить **SNS Topic** для отправки уведомлений (например, в **Slack**).

#### Создание SNS Topic:

1. Перейдите в **Amazon SNS** (Simple Notification Service) в AWS Console.
2. Нажмите **Create topic**.
3. Выберите тип темы **Standard**.
4. Укажите имя темы, например, **HighCPUAlert**.
5. Нажмите **Create topic**.

#### Подключение Slack к SNS:

Чтобы уведомление приходило в **Slack**, выполните следующие действия:

1. Перейдите в **Slack**, создайте **Incoming Webhook** (Интеграции > Webhooks).
2. Скопируйте URL Webhook.

#### Создание подписки для SNS:

1. В **SNS** перейдите в созданную тему **HighCPUAlert**.
2. Нажмите **Create subscription**.
3. В поле **Protocol** выберите **HTTPS**.
4. В поле **Endpoint** вставьте URL Webhook для Slack.
5. Нажмите **Create subscription**.

Теперь SNS будет отправлять уведомления в ваш канал Slack при срабатывании alarm.

### 4. **Подключение SNS к CloudWatch Alarm** 🔔

1. Возвращаемся в **CloudWatch Alarm**.
2. В настройках **Actions** выберите созданный SNS Topic (**HighCPUAlert**).
3. Завершаем настройку и создаем Alarm.

### 5. **Тестирование Alarm** 🔥

1. Убедитесь, что пороговое значение установлено правильно.
2. Можете вручную запустить тест с помощью команды **AWS CLI** или инструмента **CloudWatch** для проверки реакции.

### Пример конфигурации CloudWatch Alarm через AWS CLI

Для создания такого же alarm через **AWS CLI**, выполните следующую команду:

```bash
aws cloudwatch put-metric-alarm     --alarm-name "HighCPUUtilization"     --metric-name "CPUUtilization"     --namespace "AWS/EC2"     --statistic "Average"     --period 300     --threshold 80     --comparison-operator "GreaterThanThreshold"     --evaluation-periods 2     --alarm-actions "arn:aws:sns:us-east-1:123456789012:HighCPUAlert"     --ok-actions "arn:aws:sns:us-east-1:123456789012:HighCPUAlert"     --unit "Percent"     --dimensions "Name=InstanceId,Value=i-1234567890abcdef0"
```

Здесь:

- **`--alarm-name`**: имя alarm.
- **`--threshold`**: порог (80%).
- **`--alarm-actions`**: действия (например, SNS Topic для уведомления).
- **`--evaluation-periods`**: период (в данном случае, 2 периода по 5 минут).

## Действия при срабатывании Alarm 🔥

Когда срабатывает alarm, **CloudWatch** может выполнить различные действия:

- **Отправка уведомлений через SNS**: Уведомления могут быть отправлены на электронную почту, SMS, Slack, или другие каналы.
- **Запуск Lambda**: Запуск автоматических скриптов или процессов с помощью **AWS Lambda** (например, для перезапуска серверов или масштабирования).
- **Масштабирование**: Если alarm связан с **Auto Scaling Group**, это может вызвать автоматическое масштабирование ресурсов.

## Резюме: Как настроить Typical Alert в CloudWatch

| Шаг                             | Описание                                                                     |
| ------------------------------- | ---------------------------------------------------------------------------- |
| **1. Создание Alarm**           | Выберите метрику для отслеживания (например, **CPU Utilization**)            |
| **2. Установка порога**         | Установите порог, при котором должно срабатывать оповещение (например, >80%) |
| **3. Настройка действия**       | Выберите действие, например, уведомление через **SNS** или запуск **Lambda** |
| **4. Подтверждение и создание** | Просмотрите и подтвердите настройки, затем нажмите **Create alarm**          |

### Пример настройки в AWS Console:

1. **Создание CloudWatch Alarm** с порогом на **80%** загрузки CPU.
2. **Подключение SNS Topic** для отправки уведомлений на электронную почту или SMS.
3. **Подтверждение и активация**.

Теперь, если нагрузка на CPU превышает 80%, CloudWatch отправит уведомление через SNS, и вы будете в курсе проблем с сервером. 🚀

</p></details>

---

# 3. IaC, Terraform

## ❓ 1. Что такое Infrastructure as Code (IaC) и как реализуется IaC в AWS?

**[📺 Видео-вопрос (21:32)](https://youtu.be/5-MVBKH77tE?t=1292)**

<details><summary><strong>Показать ответ</strong></summary><p>

**Infrastructure as Code (IaC)** — это подход к управлению и развертыванию инфраструктуры с использованием программных интерфейсов, таких как конфигурационные файлы и сценарии, а не ручной настройки серверов, баз данных и других инфраструктурных компонентов. В IaC вся инфраструктура описана как код, что позволяет автоматически создавать, изменять и управлять инфраструктурой с помощью инструментов, таких как Terraform, AWS CloudFormation и других.

## Основные принципы **IaC**:

- **Версионность**: Как и любой другой код, инфраструктура может быть версионирована, что позволяет отслеживать изменения, восстанавливать предыдущие версии и управлять зависимостями.
- **Автоматизация**: Вместо ручного развертывания серверов или настройки сети, IaC позволяет автоматизировать все процессы через код.
- **Предсказуемость**: При повторном запуске кода результат будет неизменным, что обеспечивает стабильность и воспроизводимость среды.
- **Документирование**: Код инфраструктуры служит как документ, в котором описаны все компоненты и их связи.

## **Как реализуется IaC в AWS?**

AWS поддерживает несколько популярных инструментов для реализации **IaC**. Рассмотрим наиболее популярные подходы:

### **1. AWS CloudFormation** 📜

**AWS CloudFormation** — это нативный инструмент AWS для управления инфраструктурой как код. С помощью CloudFormation вы можете описать все ресурсы AWS (например, EC2, RDS, S3, Lambda, VPC) в виде **JSON** или **YAML** шаблонов и автоматизировать их развертывание и управление.

#### Преимущества:

- Полностью интегрирован с AWS, поддерживает все ресурсы.
- Простой способ создавать и управлять большим количеством ресурсов AWS.
- Позволяет использовать **StackSets** для управления многими регионами и учетными записями AWS.

#### Пример CloudFormation шаблона (YAML):

```yaml
AWSTemplateFormatVersion: '2010-09-09'
Resources:
  MyS3Bucket:
    Type: 'AWS::S3::Bucket'
    Properties:
      BucketName: 'my-infrastructure-as-code-bucket'
```

В данном примере создается **S3 Bucket** с указанным именем.

#### Как это работает:

1. Вы пишете шаблон в **YAML** или **JSON** формате.
2. CloudFormation автоматически создает все указанные ресурсы.
3. Вы можете легко обновлять и изменять инфраструктуру, не вмешиваясь в каждую деталь.

### **2. Terraform** 🔧

**Terraform** — это инструмент с открытым исходным кодом от HashiCorp для управления инфраструктурой как код, который поддерживает различные облачные провайдеры, включая AWS, Azure, GCP и другие.

#### Преимущества:

- **Многооблачность**: Terraform поддерживает не только AWS, но и другие облачные провайдеры.
- **Планирование изменений**: Перед применением изменений Terraform показывает, что будет изменено, что помогает избежать ошибок.
- **Модульность**: Возможность создавать повторно используемые модули для различных проектов.

#### Пример Terraform конфигурации для создания S3 Bucket:

```hcl
provider "aws" {
  region = "us-west-2"
}

resource "aws_s3_bucket" "my_bucket" {
  bucket = "my-infrastructure-as-code-bucket"
  acl    = "private"
}
```

#### Как это работает:

1. Вы пишете конфигурацию в формате **HCL** (HashiCorp Configuration Language).
2. Используя команду **terraform apply**, Terraform создает указанные ресурсы.
3. Terraform также поддерживает управление состоянием, позволяя следить за текущими изменениями инфраструктуры.

### **3. AWS CDK (Cloud Development Kit)** 🌲

**AWS CDK** — это набор инструментов для разработки инфраструктуры в AWS с использованием популярных языков программирования, таких как Python, TypeScript, Java и C#.

#### Преимущества:

- **Языковая гибкость**: CDK поддерживает различные языки программирования, что позволяет разрабатывать инфраструктуру с использованием привычных инструментов.
- **Объектно-ориентированное программирование**: Вы можете создавать компоненты инфраструктуры, используя объектно-ориентированный подход.
- **Автоматическая генерация CloudFormation**: CDK генерирует шаблоны CloudFormation, что упрощает развертывание.

#### Пример AWS CDK для создания S3 Bucket:

```python
from aws_cdk import core
import aws_cdk.aws_s3 as s3

class MyS3BucketStack(core.Stack):
    def __init__(self, scope: core.Construct, id: str, **kwargs) -> None:
        super().__init__(scope, id, **kwargs)

        s3.Bucket(self, "MyS3Bucket",
                  bucket_name="my-infrastructure-as-code-bucket")
```

#### Как это работает:

1. Вы пишете код на одном из поддерживаемых языков программирования.
2. AWS CDK генерирует **CloudFormation** шаблон, который используется для создания ресурсов.
3. CDK упрощает создание более сложных архитектур и интеграцию с другими сервисами AWS.

### **4. Ansible** ⚙️

**Ansible** — это еще один инструмент для управления инфраструктурой как код, который позволяет управлять серверами и облачными ресурсами, в том числе через AWS.

#### Преимущества:

- **Простота использования**: Ansible использует простой синтаксис YAML для описания инфраструктуры.
- **Отсутствие агентов**: Ansible работает без агентов на целевых машинах.
- **Широкая поддержка облаков**: Интеграция с AWS и другими облачными провайдерами.

#### Пример использования Ansible для создания S3 Bucket:

```yaml
- name: Create an S3 bucket
  hosts: localhost
  tasks:
    - name: Create S3 bucket
      aws_s3:
        bucket: my-infrastructure-as-code-bucket
        region: us-west-2
        state: present
```

#### Как это работает:

1. Вы пишете **Playbook** в **YAML**.
2. Ansible выполняет команды для создания ресурсов на AWS с помощью **AWS CLI**.

## **Преимущества использования IaC в AWS**:

- **Автоматизация**: Все процессы создания и изменения инфраструктуры автоматизируются, что уменьшает риск ошибок.
- **Версионность**: Поддержка версий конфигурации позволяет отслеживать изменения инфраструктуры и откатывать к предыдущим версиям.
- **Воспроизводимость**: Код инфраструктуры можно запускать несколько раз, и результат всегда будет одинаковым.
- **Масштабируемость**: Легко масштабировать инфраструктуру, добавляя или изменяя ресурсы с помощью кода.

## **Резюме**: Реализация **IaC** в AWS

| Инструмент         | Описание                                                                                                |
| ------------------ | ------------------------------------------------------------------------------------------------------- |
| **CloudFormation** | Нативный инструмент AWS для создания и управления ресурсами через шаблоны.                              |
| **Terraform**      | Многооблачный инструмент с поддержкой различных провайдеров и гибкой конфигурацией.                     |
| **AWS CDK**        | Инструмент для разработки инфраструктуры через код с использованием популярных языков программирования. |
| **Ansible**        | Простой инструмент для управления облачной инфраструктурой с использованием YAML.                       |

</p></details>

---

## ❓ 2. Ты бы хранил код Terraform в том же репозитории, что и код сервиса?

**[📺 Видео-вопрос (25:34)](https://youtu.be/5-MVBKH77tE?t=1534)**

<details><summary><strong>Показать ответ</strong></summary><p>

> При использовании **Terraform** для автоматического развертывания инфраструктуры, возникает вопрос: где хранить код Terraform? Два подхода: хранение кода рядом с приложением или в отдельном репозитории. Оба метода имеют свои плюсы и минусы. Рассмотрим оба подхода и их особенности.

## **1. Terraform код рядом с кодом сервиса** 📂

**Описание**: В этом подходе весь код (приложение и инфраструктура) хранится в одном репозитории.

### Преимущества:

- **Упрощенная синхронизация**: Изменения в приложении и инфраструктуре можно вносить одновременно, что упрощает процесс.
- **Целостность**: Вся инфраструктура и код находятся в одном месте, что упрощает контроль версий.
- **Автоматизация CI/CD**: Легче настроить pipeline для деплоя, который включает как код приложения, так и инфраструктуру.

### Минусы:

- **Рост репозитория**: Репозиторий может быстро становиться очень большим, что усложняет его управление.
- **Безопасность**: Инфраструктура может содержать чувствительные данные, такие как ключи доступа и пароли.
- **Меньше гибкости для команд**: Несколько команд могут работать с инфраструктурой и кодом одновременно, что может привести к конфликтам.

## **2. Terraform код в отдельном репозитории** 🗂️

**Описание**: В этом подходе код Terraform хранится в отдельном репозитории, не связанном с кодом приложения.

### Преимущества:

- **Разделение ответственности**: Команды, работающие над приложением, не изменяют инфраструктуру, и наоборот. Это снижает вероятность конфликтов.
- **Лучшее управление версиями**: Конфигурация инфраструктуры и код приложения развиваются отдельно, что дает больше контроля.
- **Гибкость**: Можно использовать одну инфраструктуру для нескольких приложений или сервисов.

### Минусы:

- **Сложность синхронизации**: Нужно будет синхронизировать изменения инфраструктуры с изменениями приложения.
- **Дополнительные шаги в CI/CD**: Для обновлений инфраструктуры потребуется настроить отдельные pipeline.
- **Сложности с версионированием**: Нужно отслеживать версии приложения и инфраструктуры отдельно, что может вызвать дополнительные проблемы.

## **3. Гибридный подход** ⚖️

**Описание**: В этом подходе для некоторых частей инфраструктуры создаются отдельные репозитории, а остальная часть инфраструктуры остается в репозитории с кодом приложения.

### Преимущества:

- **Модульность**: Легче управлять инфраструктурой, разделив её на модули, например, для базы данных, сети и других сервисов.
- **Контроль доступа**: Возможность ограничить доступ к отдельным частям инфраструктуры для разных команд.

### Минусы:

- **Дополнительное управление**: Управление несколькими репозиториями требует больше усилий и координации.
- **Координация изменений**: При изменении одной части инфраструктуры необходимо координировать изменения в других частях, чтобы избежать проблем с совместимостью.

## **Резюме: Какой подход выбрать?**

| **Подход**                            | **Преимущества**                                                   | **Минусы**                                                              |
| ------------------------------------- | ------------------------------------------------------------------ | ----------------------------------------------------------------------- |
| **Terraform рядом с кодом**           | - Упрощенная синхронизация                                         | - Большой репозиторий, сложность управления                             |
|                                       | - Целостность, одновременные изменения приложения и инфраструктуры | - Меньше гибкости для команд                                            |
| **Terraform в отдельном репозитории** | - Разделение ответственности, безопасность                         | - Сложность синхронизации и управления двумя pipeline                   |
|                                       | - Централизованное управление инфраструктурой                      | - Требуется дополнительная настройка для CI/CD                          |
| **Гибридный подход**                  | - Модульность, лучшее разделение по частям                         | - Дополнительное управление и координация изменений между репозиториями |

## **Рекомендации**:

- **Terraform рядом с кодом** подходит для небольших проектов с маленькой командой, где нет необходимости в жестком разделении кода и инфраструктуры.
- **Terraform в отдельном репозитории** — идеальный выбор для крупных команд и проектов, где требуется масштабируемость и безопасность.
- **Гибридный подход** может быть использован, когда нужно разделить инфраструктуру на несколько частей, которые могут развиваться независимо.

</p></details>

---

## ❓ 3. Как организовать процесс для трех инженеров, чтобы они могли управлять инфраструктурой двух EC2 машин с помощью Terraform в одном репозитории, настроить новый AWS аккаунт и автоматизировать все изменения через CI/CD, исключив ручные изменения через сайт AWS?

**[📺 Видео-вопрос (26:06)](https://youtu.be/5-MVBKH77tE?t=1566)**

<details><summary><strong>Показать ответ</strong></summary><p>

> Для эффективного управления инфраструктурой двух EC2 машин с помощью Terraform, настройки нового AWS аккаунта и автоматизации всех изменений через CI/CD, без ручных изменений через сайт AWS, можно организовать следующий процесс.

## 1. 🛠️ **Разделение ролей и обязанностей**

Каждому инженеру следует назначить четкие задачи, чтобы их работа была эффективной:

- **Инженер 1 (Основной разработчик Terraform):** Ответственный за создание и обновление инфраструктуры через Terraform. Он также должен следить за архитектурными изменениями, например, добавлением или удалением ресурсов.
- **Инженер 2 (Безопасность):** Настройка IAM ролей, политик безопасности, Security Groups, S3 для хранения состояния и секретов.
- **Инженер 3 (CI/CD и автоматизация):** Разработка и настройка пайплайнов CI/CD для автоматического применения изменений, мониторинг и обработка ошибок в пайплайне.

## 2. 📂 **Структура репозитория**

Рекомендуется использовать следующую структуру репозитория для разделения ролей:

```
/modules
    /ec2/                  # Модуль EC2 для настройки виртуальных машин
    /vpc/                  # Модуль для настройки сети
    /security_group/       # Модуль для настройки безопасности
main.tf                    # Основной файл для конфигурации ресурсов
variables.tf               # Переменные для параметризации
outputs.tf                 # Определение выходных данных
terraform.tfvars           # Значения переменных для окружений
backend.tf                 # Конфигурация backend (S3, DynamoDB)
ci_cd_pipeline.tf          # Конфигурация для CI/CD
```

### Важные файлы:

- **`main.tf`** — основные ресурсы, такие как EC2 машины, VPC, и Security Groups.
- **`variables.tf`** — глобальные переменные (например, имя EC2, тип инстанса).
- **`backend.tf`** — настройка backend (используйте S3 для хранения состояния).
- **`ci_cd_pipeline.tf`** — конфигурация для автоматизации с использованием CI/CD.

## 3. 🏗️ **Настройка нового AWS аккаунта и AWS IAM**

1. **Создание нового аккаунта AWS:**

   - Зарегистрируйте новый AWS аккаунт через [AWS Console](https://aws.amazon.com/).
   - Создайте IAM пользователей с ограниченными правами для инженеров.

2. **Настройка IAM ролей и политик:**

   - Создайте роли с минимально необходимыми правами для работы с EC2, VPC, и другими ресурсами.
   - Пример политики для создания EC2 и управления VPC:

   ```json
   {
     "Version": "2012-10-17",
     "Statement": [
       {
         "Effect": "Allow",
         "Action": [
           "ec2:Describe*",
           "ec2:RunInstances",
           "ec2:TerminateInstances",
           "ec2:CreateSecurityGroup",
           "ec2:AuthorizeSecurityGroupIngress",
           "ec2:RevokeSecurityGroupIngress"
         ],
         "Resource": "*"
       }
     ]
   }
   ```

3. **Настройка S3 и DynamoDB для Terraform:**

   - Создайте S3 бакет для хранения состояния Terraform.
   - Настройте DynamoDB для блокировки состояния (чтобы избежать параллельных изменений).

   Пример конфигурации `backend.tf`:

   ```hcl
   terraform {
     backend "s3" {
       bucket = "my-terraform-state"
       key    = "path/to/my/key"
       region = "us-west-2"
     }
   }
   ```

## 4. 🚀 **CI/CD Пайплайн для автоматизации изменений через Terraform**

Для автоматизации развертывания через Terraform в CI/CD пайплайне, можно использовать такие инструменты, как **GitHub Actions**, **GitLab CI**, **Jenkins**.

### Пример пайплайна с GitHub Actions:

1. Создайте файл `.github/workflows/terraform.yml` в репозитории.
2. Пайплайн должен выполнять следующие шаги:
   - **`terraform init`** — Инициализация Terraform и подключение к S3 для состояния.
   - **`terraform plan`** — Генерация плана изменений, чтобы увидеть, что будет изменено.
   - **`terraform apply`** — Применение изменений, если план изменений прошел успешно.
   - **`terraform validate`** — Проверка конфигурации на корректность.

Пример конфигурации для GitHub Actions:

```yaml
name: Terraform CI/CD Pipeline

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  terraform:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up Terraform
        uses: hashicorp/setup-terraform@v1

      - name: Terraform Init
        run: terraform init -backend-config="bucket=my-terraform-state" -backend-config="key=path/to/my/key"

      - name: Terraform Validate
        run: terraform validate

      - name: Terraform Plan
        run: terraform plan

      - name: Terraform Apply
        run: terraform apply -auto-approve
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          AWS_DEFAULT_REGION: us-west-2
```

### Примечания:

- **AWS Secrets**: Убедитесь, что ваши AWS ключи (AWS_ACCESS_KEY_ID и AWS_SECRET_ACCESS_KEY) добавлены в секреты GitHub Actions для безопасного доступа к AWS.
- **Terraform Plan и Apply**: Убедитесь, что `terraform apply` выполняется только в случае, если изменения прошли проверку и вам нужно применить их в инфраструктуре.

## 5. 🔄 **Мониторинг и уведомления**

Для мониторинга изменений и уведомлений в случае ошибок:

- Настройте **CloudWatch** для мониторинга состояния EC2.
- Настройте уведомления через **SNS** или **Slack** о завершении пайплайнов, успешных или ошибочных применениях изменений.

## 6. 🚧 **Рекомендации по безопасности**

- Используйте **least privilege** (наименьшие привилегии) для IAM ролей и пользователей.
- Ограничьте доступ к S3 бакету и DynamoDB.
- Регулярно обновляйте и проверяйте Terraform конфигурации и политики безопасности.

</p></details>

---

## ❓ 4. Как осуществляется авторизация с двухфакторной аутентификацией в консоли AWS?

**[📺 Видео-вопрос (31:39)](https://youtu.be/5-MVBKH77tE?t=1899)**

<details><summary><strong>Показать ответ</strong></summary><p>

> В этом процессе используется **временный токен**, который генерируется через устройство MFA (например, Google Authenticator или аппаратный токен).

## 1. 🛠️ **Настройка MFA в AWS**

Перед тем как использовать MFA в AWS CLI, необходимо настроить MFA на вашей учетной записи AWS.

1. Перейдите в **AWS Management Console**.
2. В правом верхнем углу выберите **"My Security Credentials"**.
3. В разделе **Multi-Factor Authentication (MFA)** нажмите **"Assign MFA device"**.
4. Выберите тип устройства:
   - **Virtual MFA device** (например, Google Authenticator).
   - **U2F security key** (например, YubiKey).
5. Следуйте инструкциям на экране, чтобы отсканировать QR-код с помощью вашего мобильного приложения и завершить настройку.

Теперь ваше устройство MFA привязано к вашей учетной записи AWS.

## 2. 📂 **Конфигурация AWS CLI**

Для того чтобы работать с AWS CLI, необходимо выполнить начальную настройку, если вы этого еще не сделали:

1. Установите AWS CLI (подробная инструкция [здесь](https://docs.aws.amazon.com/cli/latest/userguide/install-cliv2.html)).
2. Выполните команду для настройки CLI:

```bash
aws configure
```

Заполните параметры:

- **AWS Access Key ID**
- **AWS Secret Access Key**
- **Default region name** (например, `us-east-1`)
- **Default output format** (например, `json`)

## 3. 🔑 **Получение временных учетных данных с использованием MFA**

Для работы с MFA в AWS CLI необходимо получить временные учетные данные, используя команду `sts get-session-token`.

### Пример команды для получения временных данных:

```bash
aws sts get-session-token --serial-number arn:aws:iam::ACCOUNT_ID:mfa/YourMFADeviceName --token-code YOUR_MFA_CODE
```

Где:

- **arn:aws:iam::ACCOUNT_ID:mfa/YourMFADeviceName** — это ARN вашего устройства MFA.
- **YOUR_MFA_CODE** — одноразовый код, который генерируется вашим устройством MFA (например, через Google Authenticator).

Пример:

```bash
aws sts get-session-token --serial-number arn:aws:iam::123456789012:mfa/johndoe --token-code 123456
```

### Пример ответа:

```json
{
  "Credentials": {
    "AccessKeyId": "ASIA...EXAMPLE",
    "SecretAccessKey": "wJalrXU...EXAMPLE",
    "SessionToken": "FwoGZX...EXAMPLE",
    "Expiration": "2025-08-27T12:00:00Z"
  }
}
```

В ответе будут следующие поля:

- **AccessKeyId** — временный ключ доступа.
- **SecretAccessKey** — временный секретный ключ.
- **SessionToken** — временный токен сессии.
- **Expiration** — время, до которого действуют эти учетные данные.

## 4. ⚙️ **Использование временных учетных данных в AWS CLI**

Для использования временных учетных данных в AWS CLI, нужно установить их в конфигурации:

1. Используйте команды:

```bash
aws configure set aws_access_key_id "ASIA...EXAMPLE"
aws configure set aws_secret_access_key "wJalrXU...EXAMPLE"
aws configure set aws_session_token "FwoGZX...EXAMPLE"
```

2. Теперь вы можете выполнять команды AWS CLI с этими временными данными. Например:

```bash
aws s3 ls
```

## 5. 📂 **Использование профилей для временных учетных данных**

Для удобства, можно создать профиль для использования временных данных:

```bash
aws configure set aws_access_key_id "ASIA...EXAMPLE" --profile mfa-session
aws configure set aws_secret_access_key "wJalrXU...EXAMPLE" --profile mfa-session
aws configure set aws_session_token "FwoGZX...EXAMPLE" --profile mfa-session
```

Теперь, чтобы использовать временные данные с этим профилем, выполните команду:

```bash
aws s3 ls --profile mfa-session
```

## 6. 🔔 **Рекомендации по безопасности**

- **Минимальные привилегии**: Используйте принцип наименьших привилегий для IAM ролей и пользователей.
- **Хранение секретов**: Храните ключи AWS (AWS_ACCESS_KEY_ID и AWS_SECRET_ACCESS_KEY) в надежном месте, например, в **AWS Secrets Manager**.
- **Регулярные обновления**: Обновляйте устройства MFA и пересматривайте права доступа.

</p></details>

---

# 4. AWS

## ❓ 1. Чем отличается gp2 volume от gp3 volume?

**[📺 Видео-вопрос (34:18)](https://youtu.be/5-MVBKH77tE?t=2058)**

<details><summary><strong>Показать ответ</strong></summary><p>

> В **Amazon Elastic Block Store (EBS)** существуют различные типы томов для хранения данных, среди которых **gp2** и **gp3** — это два типа **General Purpose SSD (SSD)** томов. Оба типа используются для повседневных рабочих нагрузок, но имеют ряд отличий.

## 1. **gp2 Volume** 📦

**gp2** — это старый тип томов, который использовался в AWS для общих целей. Он обеспечивает хорошую производительность и подходит для большинства рабочих нагрузок.

- **Производительность**:
  - Имеет **базовую производительность** в 3 IOPS на гигабайт (например, для 100 ГБ объема это будет 300 IOPS).
  - Производительность увеличивается пропорционально размеру тома, но существует предел в **16,000 IOPS** и **250 MB/s**.
- **Стоимость**:
  - Стоимость **gp2** томов основана на объеме, который вы используете, и размере тома влияет на максимальное количество IOPS.
- **Основные особенности**:
  - Стандартный тип для большинства рабочих нагрузок.
  - Хорош для баз данных, разработки и тестирования приложений.

## 2. **gp3 Volume** 🚀

**gp3** — это более новый и усовершенствованный тип томов, который был введен AWS, чтобы предоставить **большую гибкость** и улучшенную производительность по сравнению с **gp2**.

- **Производительность**:
  - **gp3** предлагает **постоянные IOPS и пропускную способность**, независимо от размера тома.
  - **IOPS** на **gp3** можно настроить в пределах от **3,000 до 16,000 IOPS**, и это можно сделать независимо от объема.
  - Пропускная способность **gp3** может достигать **1,000 MB/s**.
- **Стоимость**:

  - **gp3** дешевле по сравнению с **gp2** при аналогичных характеристиках.
  - **gp3** позволяет отдельно платить за IOPS и пропускную способность, что дает больше контроля и гибкости в настройке.

- **Основные особенности**:
  - Большая гибкость в настройках IOPS и пропускной способности.
  - Идеален для приложений с интенсивными операциями ввода-вывода (например, базы данных, аналитика, кэширование).
  - **gp3** позволяет снизить затраты, не теряя в производительности.

## 3. **Сравнение: gp2 vs gp3** 🔍

| **Параметр**                     | **gp2**                            | **gp3**                                       |
| -------------------------------- | ---------------------------------- | --------------------------------------------- |
| **Производительность**           | 3 IOPS/ГБ, до 16,000 IOPS          | От 3,000 до 16,000 IOPS                       |
| **Макс. Пропускная способность** | 250 MB/s                           | 1,000 MB/s                                    |
| **Стоимость**                    | Дороже при высокой нагрузке        | Дешевле при аналогичных характеристиках       |
| **Настройка IOPS**               | Автоматическая, зависит от размера | Можно настраивать отдельно от размера         |
| **Оптимальные сценарии**         | Общие приложения, тестирование     | Высокопроизводительные базы данных, аналитика |

## 4. **Заключение** 📝

- **gp2** — это более старый тип томов, который предоставляет базовую производительность в зависимости от размера тома.
- **gp3** — это более гибкий и дешевый тип томов с возможностью настройки производительности, который является лучшим выбором для высоконагруженных приложений и тех, кто хочет контролировать расходы.

Если вам нужна большая гибкость и возможность точной настройки IOPS и пропускной способности, **gp3** — лучший выбор! 💡

</p></details>

---

## ❓ 2. В чем разница между NAT Gateway и Internet Gateway в AWS?

**[📺 Видео-вопрос (35:08)](https://youtu.be/5-MVBKH77tE?t=2108)**

<details><summary><strong>Показать ответ</strong></summary><p>

> В AWS существуют два ключевых компонента для работы с интернет-соединением: **Internet Gateway (IGW)** и **NAT Gateway (NAT GW)**. Они выполняют разные функции, и их использование зависит от типа сетевой архитектуры и безопасности.

## 1. 🌍 **Internet Gateway (IGW)**

**Назначение:** обеспечивает **двусторонний доступ** между VPC и интернетом.  
**Применение:** используется для обеспечения **публичного доступа** из VPC в интернет и наоборот. Например, для веб-серверов, которые должны быть доступны из интернета.

### Характеристики:

- **Подключается к VPC.**
- Используется для **публичных subnet**.
- Позволяет ресурсам (например, EC2) иметь **публичный IP** и прямой доступ к интернету.
- Не выполняет NAT, а просто маршрутизирует трафик.

### Пример:

- Веб-сервер с публичным IP, который принимает запросы из интернета.

## 2. 🔄 **NAT Gateway (NAT GW)**

**Назначение:** предоставляет **выход в интернет** для ресурсов в **приватной subnet**, без необходимости иметь публичные IP.  
**Применение:** когда приватные ресурсы (например, базы данных, backend-серверы) должны **делать исходящие запросы в интернет**, но не быть доступными для входящих подключений.

### Характеристики:

- Размещается в **публичной subnet** с IGW.
- **Маршрутизирует трафик** из приватной subnet в интернет.
- **Блокирует входящий трафик** по умолчанию.
- Платная услуга, стоимость зависит от времени работы и трафика.

### Пример:

- Приватная EC2, которая должна обновлять пакеты через интернет, но не должна быть доступна извне.

## 3. ⚡ **Ключевая разница**

| Особенность                  | Internet Gateway (IGW)   | NAT Gateway (NAT GW)         |
| ---------------------------- | ------------------------ | ---------------------------- |
| **Доступность**              | Публичный интернет ↔ VPC | Приватные ресурсы → интернет |
| **Публичный IP**             | Да                       | Да, но только для выхода     |
| **Использование**            | Публичные subnet         | Приватные subnet             |
| **Входящий трафик**          | Разрешен                 | Блокирован по умолчанию      |
| **Прямой доступ к ресурсам** | Да                       | Нет                          |

## 4. 💡 **Пример схемы**

**Public Subnet:** EC2 + IGW → интернет.  
**Private Subnet:** EC2 → NAT Gateway (в Public Subnet) → IGW → интернет.

С помощью **Internet Gateway** можно обеспечить прямое соединение между **публичными ресурсами** и интернетом, в то время как **NAT Gateway** предоставляет **приватным ресурсам** выход в интернет, но не дает доступ извне.

---

</p></details>

---

## ❓ 3. Можно ли получить доступ к ресурсам одной VPC из другой VPC?

**[📺 Видео-вопрос (36:08)](https://youtu.be/5-MVBKH77tE?t=2168)**

<details><summary><strong>Показать ответ</strong></summary><p>

> Да, в AWS можно получить доступ к ресурсам одной VPC из другой VPC. Для этого существует несколько методов, каждый из которых имеет свои особенности и применения.

## 1. 🔗 **VPC Peering (Пэринг VPC)**

**Назначение:** создание прямого соединения между двумя VPC, позволяющее обмениваться данными между ресурсами.

### Как работает:

- **VPC Peering** позволяет устанавливать прямое соединение между двумя VPC, даже если они находятся в разных регионах.
- Это соединение **не использует интернет** для передачи данных, что делает его безопасным.
- Требуется настройка таблиц маршрутизации в обеих VPC.

### Пример:

- **Веб-сервер** в одной VPC может подключаться к **базе данных** в другой VPC через пэринг.

### Шаги настройки:

1. Создайте **VPC Peering Connection** через AWS Management Console.
2. Обновите таблицы маршрутизации обеих VPC.
3. Настройте **Security Groups** и **NACLs** для разрешения трафика.

## 2. 🌍 **Transit Gateway (TGW)**

**Назначение:** создание централизованного маршрутизатора для подключения нескольких VPC и локальных сетей.

### Как работает:

- **Transit Gateway** позволяет подключать **несколько VPC** через единый маршрутизатор.
- Применяется для масштабируемого и гибкого соединения нескольких VPC.
- Поддерживает межрегиональные соединения.

### Пример:

- Используется, если у вас много VPC, которые нужно подключить друг к другу, а также локальная сеть.

### Шаги настройки:

1. Создайте **Transit Gateway**.
2. Подключите VPC к TGW.
3. Обновите таблицы маршрутизации.

## 3. 🌐 **VPN Connection (VPN-соединение)**

**Назначение:** создание защищенного соединения через интернет между VPC.

### Как работает:

- Использует **VPN Gateway** для создания защищенного туннеля между двумя VPC или VPC и локальной сетью.
- Хорошо подходит для случаев, когда нужно подключить локальную сеть к VPC через интернет.

### Пример:

- Локальная сеть в офисе подключается к VPC в AWS через VPN для безопасного обмена данными.

### Шаги настройки:

1. Создайте **VPN Gateway**.
2. Настройте **VPN Connection** между VPC.
3. Обновите таблицы маршрутизации и политики безопасности.

## 4. 🔐 **PrivateLink**

**Назначение:** обеспечивает доступ к сервисам в другой VPC через **частное соединение** без выхода в интернет.

### Как работает:

- **PrivateLink** используется для подключения к сервисам, размещенным в другой VPC, без использования публичного интернета.
- Применяется для защищенного доступа к сервисам, таким как базы данных или API.

### Пример:

- Получение доступа к приватному **API сервису** в другой VPC без использования публичного интернета.

### Шаги настройки:

1. Создайте **VPC Endpoint Service**.
2. Создайте **VPC Endpoint** в другой VPC для доступа к сервису через PrivateLink.

## 5. 🔐 **Безопасность**

Не забудьте правильно настроить **Security Groups** и **Network Access Control Lists (NACLs)** для обеспечения безопасности трафика между VPC. Для каждого из вышеописанных методов требуется обновить таблицы маршрутизации и политики безопасности для безопасного обмена данными.

## 📊 **Итог:**

- **VPC Peering** — для **прямого соединения** между двумя VPC.
- **Transit Gateway** — для **масштабируемого соединения** нескольких VPC.
- **VPN** — для **подключения с локальной сети**.
- **PrivateLink** — для **приватного доступа** к сервисам из другой VPC.

</p></details>

---

## ❓ 4. Что такое instance profile?

**[📺 Видео-вопрос (36:39)](https://youtu.be/5-MVBKH77tE?t=2199)**

<details><summary><strong>Показать ответ</strong></summary><p>

> **Instance Profile** — это **IAM роль**, которая привязывается к **EC2 инстансу** и предоставляет ему доступ к другим AWS сервисам без необходимости вручную управлять ключами доступа. Это безопасный и эффективный способ предоставить EC2 инстансам разрешения для взаимодействия с другими ресурсами AWS.

## 1. 🌍 **Как работает Instance Profile?**

Когда вы создаете EC2 инстанс, вы можете привязать к нему **Instance Profile**. Это позволяет инстансу использовать **IAM роль**, которая содержит разрешения для доступа к ресурсам AWS, таким как **S3**, **DynamoDB**, **CloudWatch** и т. д.

- **Instance Profile** представляет собой объект **IAM Role**.
- При запуске EC2 инстанса эта роль автоматически привязывается к инстансу и предоставляет ему необходимые **разрешения**.
- EC2 инстанс использует эти разрешения для выполнения операций с другими сервисами AWS.

## 2. ⚙️ **Пример использования Instance Profile**

### Шаги для настройки:

1. **Создайте IAM роль** с необходимыми разрешениями. Например, для доступа к S3:

   - Пример политики для разрешения на чтение данных из S3:

   ```json
   {
     "Version": "2012-10-17",
     "Statement": [
       {
         "Effect": "Allow",
         "Action": "s3:GetObject",
         "Resource": "arn:aws:s3:::my-bucket/*"
       }
     ]
   }
   ```

2. **Привяжите IAM роль** к **Instance Profile** (AWS автоматически создает Instance Profile при создании роли).

3. При запуске EC2 инстанса выберите этот **Instance Profile**.

4. Теперь инстанс может **автоматически получать разрешения** для доступа к S3 или другим сервисам, указанным в политике IAM.

## 3. 🔒 **Преимущества использования Instance Profile**

- **Безопасность**: Нет необходимости вручную управлять ключами доступа. Роль автоматически передается инстансу через Instance Profile.
- **Управляемость**: Все разрешения централизованно управляются через IAM, что упрощает управление доступом.
- **Автоматическое обновление**: Если роли или политики обновляются, изменения автоматически применяются к инстансам, что упрощает управление разрешениями.

## 4. 📌 **Когда использовать Instance Profile?**

- Когда EC2 инстанс должен **взаимодействовать с другими AWS сервисами** (например, S3, DynamoDB, CloudWatch).
- Когда необходимо **управлять доступом централизованно** через IAM.
- Когда важно **избежать использования ключей доступа**, улучшая безопасность.

## 5. 🔄 **Итог**

**Instance Profile** — это механизм назначения **IAM роли** EC2 инстансу, который предоставляет доступ к другим AWS сервисам. Это позволяет избежать использования ключей доступа, улучшая безопасность и управляемость доступом.

</p></details>

---

## ❓ 5. Что такое режим Multi-AZ в Amazon RDS и как он работает?

**[📺 Видео-вопрос (32:23)](https://youtu.be/5-MVBKH77tE?t=2303)**

<details><summary><strong>Показать ответ</strong></summary><p>

> **Multi-AZ (Multiple Availability Zones)** — это режим развертывания в **Amazon RDS**, который обеспечивает **высокую доступность** и **отказоустойчивость** для базы данных, используя несколько **Availability Zones (AZs)** в одном регионе AWS. В этом режиме создается **зеркало базы данных**, которое синхронно реплицирует данные на другой Availability Zone. В случае сбоя на одной из зон, Amazon RDS автоматически переключается на **резервную базу данных** в другой зоне.

## 📈 **Как работает Multi-AZ в Amazon RDS?**

1. **Основной и резервный инстанс:** В режиме Multi-AZ Amazon RDS создаёт два инстанса базы данных:

   - **Основной инстанс** — активно обрабатывает запросы.
   - **Резервный инстанс** — находится в другом AZ и синхронно реплицирует данные с основного инстанса.

2. **Синхронная репликация:** Изменения на основном инстансе базы данных немедленно реплицируются на резервный инстанс, что обеспечивает консистентность данных.

3. **Автоматический failover:** Если основной инстанс выходит из строя (например, из-за сбоя в сети или аппаратной неисправности), Amazon RDS автоматически переключает трафик на резервный инстанс, минимизируя время простоя. При этом продолжает использоваться тот же **DNS-имя** для подключения, что упрощает процесс.

4. **Обновления и патчи:** В режиме Multi-AZ Amazon RDS может применить обновления и патчи в резервной зоне, не влияя на основную работу базы данных, и только после этого переключить основной инстанс на резервный.

## 🛠️ **Особенности и преимущества Multi-AZ:**

- **Высокая доступность и отказоустойчивость:** Автоматическое переключение на резервный инстанс при сбоях.
- **Синхронная репликация:** Все изменения в данных немедленно реплицируются, обеспечивая консистентность и актуальность данных.
- **Безопасность:** В случае сбоя доступность базы данных обеспечивается с минимальным временем простоя.
- **Упрощение управления:** Нет необходимости в ручной настройке репликации или переключении между инстансами — все автоматизируется.
- **Низкое время простоя:** В случае необходимости переключения на резервный инстанс, время простоя минимизируется.

## ⚙️ **Как настроить режим Multi-AZ в Amazon RDS?**

1. **Создание базы данных:**

   - При создании инстанса базы данных в **Amazon RDS** выберите **режим Multi-AZ**.
   - В процессе настройки AWS автоматически развернет резервный инстанс в другом Availability Zone.

2. **Автоматическое восстановление при сбое (failover):**
   - Если основной инстанс выходит из строя, система автоматически переключит трафик на резервный инстанс.
   - Резервный инстанс становится основным, и все подключения будут перенаправлены на него.

## 📌 **Когда использовать режим Multi-AZ в Amazon RDS?**

- **Приложения с критической доступностью:** Для тех, кто требует **высокой доступности и минимального времени простоя**, например, для банковских приложений, онлайн-магазинов и систем обработки транзакций.
- **Резервные инстансы:** Если ваша база данных должна оставаться доступной в любое время, несмотря на сбои в инфраструктуре.
- **Регулярные обновления:** Если ваша база данных должна быть обновлена или патчена без прерывания работы.

## 📝 **Важно помнить:**

- В режиме **Multi-AZ** резервный инстанс не используется для обработки запросов в обычном режиме, он работает только как **резервная копия**.
- Для **чтения и записи** используется только основной инстанс.
- Это решение **не подходит для нагрузок**, которые требуют **горизонтальной масштабируемости** (например, через репликацию чтения). В этом случае лучше использовать **Read Replicas**.

## 📊 **Итог**

**Multi-AZ в Amazon RDS** — это решение для обеспечения **высокой доступности** и **отказоустойчивости** базы данных с использованием **нескольких Availability Zones**. Это позволяет защитить данные от сбоев и минимизировать время простоя, что идеально подходит для критически важных приложений, где нужно, чтобы база данных была доступна всегда.

</p></details>

---

## ❓ 6. Как защититься от случайного нажатия кнопки "Terminate" в EC2?

**[📺 Видео-вопрос (41:25)](https://youtu.be/5-MVBKH77tE?t=2485)**

<details><summary><strong>Показать ответ</strong></summary><p>

## 1. 🛠️ **Включение защиты от Terminate (Termination Protection)**

**Termination Protection** позволяет заблокировать возможность удаления инстанса через AWS Management Console или AWS CLI.

### Как включить защиту от терминатора:

1. Перейдите в **AWS Management Console**.
2. Откройте раздел **EC2** и выберите инстанс, для которого хотите включить защиту.
3. Перейдите на вкладку **Actions**, выберите **Instance Settings**, затем **Change Termination Protection**.
4. Установите переключатель **Enable** для включения защиты от терминатора.
5. Нажмите **Apply**.

Теперь этот инстанс нельзя будет удалить, пока защита от терминатора не будет отключена вручную.

## 2. 🛡️ **Использование IAM политики для ограничения прав**

**IAM политики** позволяют ограничить доступ к действиям, связанным с удалением инстансов EC2.

### Пример IAM политики для запрета удаления инстансов:

```json
{
  "Version": "2012-10-17",
  "Statement": [
    {
      "Effect": "Deny",
      "Action": "ec2:TerminateInstances",
      "Resource": "*"
    }
  ]
}
```

Эта политика будет **отключать** возможность терминатора для пользователей, к которым она применяется.

</p></details>

---

# 5. Linux, Docker

## ❓ 1. Как изменить права доступа для директории?

**[📺 Видео-вопрос (45:26)](https://youtu.be/5-MVBKH77tE?t=2726)**

<details><summary><strong>Показать ответ</strong></summary><p>

> В операционных системах Linux/Unix и macOS для изменения прав доступа к директориям используются команды `chmod` и `chown`. Эти команды позволяют гибко управлять правами доступа, что важно для защиты данных и безопасности системы.

## 1. 🛠️ **Изменение прав доступа с помощью команды `chmod`**

Команда **`chmod`** (change mode) используется для изменения прав доступа к файлам и директориям.

### 📊 **Типы прав доступа:**

- **r** — право на чтение (read).
- **w** — право на запись (write).
- **x** — право на выполнение (execute).

### 🔑 **Типы пользователей:**

- **u** — владелец (user).
- **g** — группа (group).
- **o** — другие пользователи (others).
- **a** — все пользователи (all).

### 💻 **Примеры использования:**

1. Дать **владельцу** полный доступ (чтение, запись, выполнение) к директории:

   ```bash
   chmod u+rwx /path/to/directory
   ```

2. Дать **группе** право на чтение и выполнение:

   ```bash
   chmod g+rx /path/to/directory
   ```

3. Удалить **право на запись** для всех пользователей:

   ```bash
   chmod a-w /path/to/directory
   ```

4. Установить **точные права**: например, только владельцу доступно чтение и запись, а группе и остальным — только чтение:
   ```bash
   chmod 744 /path/to/directory
   ```

## 2. 🛡️ **Изменение владельца и группы с помощью команды `chown`**

Команда **`chown`** используется для изменения владельца и группы файлов и директорий.

### 📋 **Пример использования:**

1. Изменить владельца на **user1** и группу на **group1**:

   ```bash
   chown user1:group1 /path/to/directory
   ```

2. Изменить только владельца:

   ```bash
   chown user1 /path/to/directory
   ```

3. Изменить только группу:
   ```bash
   chown :group1 /path/to/directory
   ```

## 3. 🔄 **Использование команды `chmod` с опцией `-R` для рекурсивных изменений**

Если нужно изменить права доступа для директории **и всех вложенных файлов/подкаталогов**, используйте опцию `-R` (рекурсивно).

### 📋 **Пример:**

1. Установить права на **чтение, запись и выполнение** для всех файлов и подкаталогов в директории:

   ```bash
   chmod -R 755 /path/to/directory
   ```

2. Дать **владельцу** и **группе** право на чтение и выполнение, а остальным — только на чтение:
   ```bash
   chmod -R 744 /path/to/directory
   ```

## 4. 🔑 **Пример использования прав доступа с числовыми значениями**

Права можно задавать с использованием числового кода (например, 755, 644).

### 📊 **Кодировка прав:**

- **r = 4**
- **w = 2**
- **x = 1**

Сложив их, вы получаете:

- **7 (rwx)** — полный доступ.
- **6 (rw-)** — право на чтение и запись.
- **5 (r-x)** — право на чтение и выполнение.
- **4 (r--)** — право только на чтение.

### 📝 **Пример:**

1. Установить права **755** для директории:

   ```bash
   chmod 755 /path/to/directory
   ```

2. Установить права **700** (только для владельца):
   ```bash
   chmod 700 /path/to/directory
   ```

## 5. ⚠️ **Проверка прав доступа с помощью `ls -l`**

После изменения прав доступа можно проверить текущие права с помощью команды **`ls -l`**.

### Пример:

```bash
ls -l /path/to/directory
```

Это покажет права доступа, владельца и группу, а также дату и время последнего изменения.

## 📌 **Итог**

- **`chmod`** — для изменения прав доступа к файлам и директориям.
- **`chown`** — для изменения владельца и группы директории.
- **`ls -l`** — для проверки текущих прав доступа.

</p></details>

---

## ❓ 2. Что такое telnet?

**[📺 Видео-вопрос (45:41)](https://youtu.be/5-MVBKH77tE?t=2741)**

<details><summary><strong>Показать ответ</strong></summary><p>

> **Telnet** — это старый сетевой протокол, используемый для **удаленного доступа** к устройствам через текстовый интерфейс командной строки. Он позволяет пользователям подключаться к удаленному серверу или компьютеру через сеть и управлять системой с помощью команд.

## 🔑 **Основные характеристики Telnet:**

- **Протокол удаленного доступа:** Telnet используется для удаленного подключения к системам через командную строку.
- **Двусторонний текстовый интерфейс:** Пользователи могут отправлять команды и получать ответы от удаленной системы.
- **Стандартный порт:** Telnet работает по порту **23** по умолчанию.
- **Безопасность:** Telnet передает данные в **открытом виде**, не шифруя трафик, что делает его уязвимым для перехвата данных, например, паролей. Это делает его менее безопасным для использования в современных сетях.

## ⚙️ **Как работает Telnet?**

1. **Установка соединения:** Когда пользователь запускает клиент Telnet, он указывает **IP-адрес** или **доменное имя** удаленного устройства, к которому нужно подключиться.
2. **Аутентификация:** После установления соединения может запрашиваться **логин и пароль** для доступа к удаленной системе.
3. **Отправка команд:** После аутентификации пользователь может вводить команды, как если бы он сидел за локальным компьютером. Команды отправляются через **TCP-соединение** и выполняются на удаленной машине.
4. **Получение откликов:** Результаты выполнения команд возвращаются на клиентскую сторону и отображаются в терминале.

## 🛠️ **Как использовать Telnet?**

1. **Подключение к удаленному серверу:**
   Для подключения к серверу с использованием Telnet, используйте следующую команду:

   ```bash
   telnet [hostname или IP-адрес] [порт]
   ```

   Пример:

   ```bash
   telnet example.com 23
   ```

2. **Тестирование портов:**
   Telnet также используется для **тестирования доступности сервисов** по определенным портам. Например, чтобы проверить, доступен ли порт 80 на сервере:

   ```bash
   telnet example.com 80
   ```

## 🔒 **Проблемы безопасности Telnet:**

1. **Отсутствие шифрования:** Telnet **не шифрует данные**, что делает его уязвимым для атак типа **Man-in-the-Middle (MitM)**, где злоумышленники могут перехватывать передаваемые данные.
2. **Уязвимость для атак:** Поскольку Telnet отправляет данные в открытом виде, включая **пароли**, он уязвим для различных атак, таких как **перехват** и **вредоносные вмешательства**.

## 🛡️ **Современные альтернативы Telnet:**

Из-за проблем с безопасностью Telnet в основном заменен более защищенными протоколами:

- **SSH (Secure Shell):** Это безопасная альтернатива Telnet, которая использует **шифрование** для защиты данных. SSH является стандартом для удаленного доступа к серверам и машинам в современных системах.

  Пример подключения через SSH:

  ```bash
  ssh user@hostname
  ```

- **SFTP (Secure File Transfer Protocol):** Альтернатива для **безопасной передачи файлов** между устройствами.

## 📌 **Итог**

**Telnet** — это старый протокол для удаленного доступа через командную строку. Хотя он полезен для тестирования и управления устройствами, его слабая безопасность ограничивает использование, особенно в современных сетях. Для безопасного удаленного доступа рекомендуется использовать **SSH** вместо Telnet.

</p></details>

---

## ❓ 3. Чем можно помониторить ресурсы на сервере

**[📺 Видео-вопрос (52:30)](https://youtu.be/5-MVBKH77tE?t=3146)**

<details><summary><strong>Показать ответ</strong></summary><p>

## Этап 1: Общий обзор состояния системы

### 📊 Команда `top` или `htop`

```bash
top
# или, если установлен:
htop
```

**Проверяем:**

- Загрузка CPU (`%us`, `%sy`, `%id`)
- Использование памяти (RES, SWAP)
- Процессы, потребляющие больше всего ресурсов

<details><summary><strong>Полное описание команды top</strong></summary><p>

### 📊 Верхняя часть — общая сводка

### 🕒 Строка 1: Время, аптайм, пользователи, нагрузка

```
top - 14:23:45 up 3 days,  4:56,  2 users,  load average: 0.52, 0.48, 0.31
```

| Поле           | Значение                            |
| -------------- | ----------------------------------- |
| `14:23:45`     | Текущее время                       |
| `up 3 days...` | Аптайм — как долго работает система |
| `2 users`      | Количество активных пользователей   |
| `load average` | Средняя нагрузка за 1, 5, 15 минут  |

### 📋 Строка 2: Задачи (процессы)

```
Tasks: 215 total, 1 running, 211 sleeping, 3 stopped, 0 zombie
```

| Статус     | Значение                 |
| ---------- | ------------------------ |
| `total`    | Всего процессов          |
| `running`  | Активно выполняются      |
| `sleeping` | Ждут событий             |
| `stopped`  | Остановлены              |
| `zombie`   | Завершены, но не удалены |

### 🧠 Строка 3: Загрузка CPU

```
%us  %sy  %ni  %id  %wa  %hi  %si  %st
```

| Поле  | Описание                                 |
| ----- | ---------------------------------------- |
| `%us` | Пользовательская нагрузка                |
| `%sy` | Системная нагрузка                       |
| `%ni` | Nice — процессы с изменённым приоритетом |
| `%id` | Idle — простаивающее время               |
| `%wa` | Wait — ожидание ввода/вывода             |
| `%hi` | Hardware interrupts                      |
| `%si` | Software interrupts                      |
| `%st` | Steal time (на виртуальных машинах)      |

### 🧮 Строки 4–5: Память и swap

```
MiB Mem :  15929.0 total,  11054.7 used,  4874.3 free,  132.5 buffers
MiB Swap:  2048.0 total,   112.0 used,  1936.0 free.  3674.0 cached
```

| Поле      | Значение            |
| --------- | ------------------- |
| `total`   | Общий объём памяти  |
| `used`    | Используется сейчас |
| `free`    | Свободна            |
| `buffers` | Буферы для I/O      |
| `cached`  | Файловый кэш        |

### 📋 Таблица процессов (нижняя часть)

```
PID USER  PR  NI VIRT  RES  SHR S %CPU %MEM TIME+ COMMAND
```

| Поле      | Значение                        |
| --------- | ------------------------------- |
| `PID`     | ID процесса                     |
| `USER`    | Владелец процесса               |
| `PR`      | Приоритет                       |
| `NI`      | Nice-значение                   |
| `VIRT`    | Виртуальная память              |
| `RES`     | Резидентная (физическая) память |
| `SHR`     | Shared memory                   |
| `S`       | Статус: R, S, Z и т.д.          |
| `%CPU`    | Использование CPU               |
| `%MEM`    | Использование памяти            |
| `TIME+`   | Общее время работы CPU          |
| `COMMAND` | Запущенная команда              |

### ⌨️ Полезные клавиши `top`

| Клавиша | Описание              |
| ------- | --------------------- |
| `P`     | Сортировать по CPU    |
| `M`     | Сортировать по памяти |
| `c`     | Полный путь команды   |
| `k`     | Убить процесс         |
| `r`     | Изменить приоритет    |
| `q`     | Выйти                 |

### ✅ Вывод

`top` позволяет:

- Мониторить загрузку CPU, RAM и swap
- Определять активные и "тяжёлые" процессы
- Анализировать поведение системы в реальном времени

</p></details>

## Этап 2: Загрузка процессора

🔍 Команда `mpstat` (из `sysstat`)

```bash
sudo apt install sysstat
mpstat -P ALL 1 5
```

**На что смотреть:**

- `%idle` — чем ниже, тем сильнее загружен CPU
- `%usr`, `%sys` — пользовательская и системная нагрузка

### ✅ Этап 3: Использование памяти

#### 🔍 Команды `free`, `vmstat`

```bash
free -h
```

**Важно:**

- `available` — сколько памяти реально доступно
- Если активно используется `swap` → нехватка RAM

```bash
vmstat 1 5
```

### ✅ Этап 4: Проверка дисковой активности

#### 📂 Команды `iostat` и `iotop`

```bash
iostat -xz 1
```

- **`-x`** — расширенный вывод, который включает дополнительные данные, такие как **IOPS** и **время ожидания операций**.
- **`1`** — интервал обновления в секундах (в данном случае 1 секунда).

<details><summary><strong>Столбцы вывода</strong></summary><p>

| **Параметр** | **Описание**                                                                                                          |
| ------------ | --------------------------------------------------------------------------------------------------------------------- |
| **Device**   | Имя устройства (например, **`sda`**, **`mmcblk0`** и т. д.).                                                          |
| **r/s**      | Количество операций чтения в секунду (IOPS для чтения).                                                               |
| **rkB/s**    | Количество данных, прочитанных в килобайтах в секунду (скорость чтения).                                              |
| **rrqm/s**   | Количество слияний операций чтения в секунду (Read Request Merges) — слияние нескольких запросов в один.              |
| **%rrqm**    | Процент слияний чтений от общего числа операций чтения.                                                               |
| **r_await**  | Среднее время ожидания операций чтения в миллисекундах.                                                               |
| **rareq-sz** | Средний размер операции чтения в килобайтах.                                                                          |
| **w/s**      | Количество операций записи в секунду (IOPS для записи).                                                               |
| **wkB/s**    | Количество данных, записанных в килобайтах в секунду (скорость записи).                                               |
| **wrqm/s**   | Количество слияний операций записи в секунду (Write Request Merges) — слияние нескольких запросов записи.             |
| **%wrqm**    | Процент слияний записей от общего числа операций записи.                                                              |
| **w_await**  | Среднее время ожидания операций записи в миллисекундах.                                                               |
| **wareq-sz** | Средний размер операции записи в килобайтах.                                                                          |
| **d/s**      | Количество операций удаления в секунду                                                                                |
| **dkB/s**    | Количество данных, удалённых в килобайтах в секунду                                                                   |
| **drqm/s**   | Количество слияний операций удаления в секунду.                                                                       |
| **%drqm**    | Процент слияний удалений от общего числа операций удаления.                                                           |
| **d_await**  | Среднее время ожидания операций удаления в миллисекундах.                                                             |
| **dareq-sz** | Средний размер операции удаления в килобайтах.                                                                        |
| **f/s**      | Количество операций файловых операций в секунду                                                                       |
| **f_await**  | Среднее время ожидания файловых операций в миллисекундах.                                                             |
| **aqu-sz**   | Средняя длина очереди ввода-вывода — показывает, сколько операций ввода-вывода ожидают своей обработки.               |
| **%util**    | Процент времени, когда устройство было занято обработкой операций ввода-вывода Это показатель нагрузки на устройство. |

</p></details>

### Примечания:

- **r/s** и **w/s**: Если эти значения равны **0.00**, это означает, что в данный момент на устройстве не выполняются операции чтения или записи.
- Если **%util** равно **0.00**, это означает, что устройство не используется для ввода-вывода и не обрабатывает запросы.

```bash
sudo iotop
```

### ✅ Этап 5: Проверка сетевой активности

### 🌐 Команда `iftop` или `nload`

```bash
sudo apt install iftop nload
sudo iftop -i eth0
nload
```

**Что искать:**

- Подозрительные входящие/исходящие потоки
- Высокая загрузка интерфейса

### ✅ Этап 6: Проверка процессов и нагрузок

### 🔍 `ps`, `pidstat`, `strace`, `perf`

```bash
ps aux --sort=-%cpu | head
ps aux --sort=-%mem | head
```

```bash
pidstat -p <PID> 1
```

Если процесс "завис" — можно проследить системные вызовы:

```bash
strace -p <PID>
```

### 🧠 Общая тактика

| Проверка            | Инструмент                           |
| ------------------- | ------------------------------------ |
| Загрузка CPU        | `top`, `htop`, `mpstat`              |
| Память              | `free`, `vmstat`                     |
| Диск                | `iostat`, `iotop`                    |
| Сеть                | `iftop`, `nload`                     |
| Конкретные процессы | `ps`, `pidstat`, `strace`, `lsof`    |
| Приложение / БД     | Логи, профилировщики, мониторинг APM |

## 💡 Вывод

> Всегда начинай с **общей картины**: CPU, память, диск, сеть.  
> Затем переходи к **конкретным процессам или сервисам**, используя `pidstat`, `strace`, `iotop`.  
> Хорошая диагностика = минимизация слепых зон.

</p></details>

---

## ❓ 4. Что такое Load Average?

**[📺 Видео-вопрос (53:00)](https://youtu.be/5-MVBKH77tE?t=3180)**

<details><summary><strong>Показать ответ</strong></summary><p>

> **Load average** — это показатель, который используется для измерения нагрузки на процессор системы за определенный промежуток времени. Он отображает среднее количество процессов, которые ожидают выполнения в очереди на процессор, включая те, которые активно выполняются, и те, которые ждут своих ресурсов (например, в ожидании ввода/вывода).

## ⚙️ **Как работает Load Average?**

- **Load average** не просто показывает количество работающих процессов, а также учитывает те процессы, которые ожидают выполнения.
- Если значение **load average** больше числа ядер в системе, это может означать, что система перегружена и процессы начинают задерживаться.

## 🕒 **Три временных интервала:**

- **1 минута**: Среднее количество процессов, ожидающих выполнения за последние 1 минуту.
- **5 минут**: Среднее количество процессов за последние 5 минут.
- **15 минут**: Среднее количество процессов за последние 15 минут.

## 📏 **Единицы измерения**

**Load average** измеряется как **число**, которое показывает количество процессов, ожидающих выполнения в текущий момент времени (или среднее значение за указанный интервал времени).

## 💻 **Пример вывода команды `uptime`:**

```bash
$ uptime
 14:05:11 up 5 days,  3:21,  2 users,  load average: 0.42, 0.58, 0.72
```

- **0.42**: Средняя нагрузка за последние 1 минуту.
- **0.58**: Средняя нагрузка за последние 5 минут.
- **0.72**: Средняя нагрузка за последние 15 минут.

## 📊 **Интерпретация Load Average**

- **1.0 на одно ядро**: Система работает на полной мощности, каждый процесс имеет достаточное время для выполнения.
- **Больше 1.0 на одно ядро**: Система перегружена, процессы начинают задерживаться.
- **Меньше 1.0 на одно ядро**: Система работает под нагрузкой, но с запасом ресурсов.

### Пример:

- **Система с 1 ядром**:
  - Если load average **1.0** — это нормальная работа.
  - Если load average **2.0** — два процесса пытаются одновременно использовать процессор.
- **Система с 4 ядрами**:
  - Если load average **4.0** — каждый процесс будет использовать по одному ядру.

## 🔧 **Использование Load Average**

- Для **мониторинга производительности** системы.
- Для **диагностики перегрузки** и принятия решений о масштабировании или оптимизации ресурсов.

</p></details>

---

## ❓ 5. Как происходит процесс контейнеризации в Linux?

**[📺 Видео-вопрос (55:34)](https://youtu.be/5-MVBKH77tE?t=3334)**

<details><summary><strong>Показать ответ</strong></summary><p>

> Контейнеризация в Linux использует несколько ключевых технологий для изоляции процессов, ресурсов и файловых систем, позволяя запускать приложения в изолированных контейнерах. Рассмотрим три важнейшие компоненты контейнеризации: **Namespaces**, **Cgroups** и **Union Filesystem**.

## 1. 🌐 **Namespaces**

**Namespaces** — это механизм, который изолирует различные ресурсы для контейнеров, такие как идентификаторы процессов, сетевая конфигурация и файловая система. Каждый контейнер получает свой изолированный **пространство имен** для этих ресурсов.

### Типы namespaces:

- **PID Namespace**: Изолирует идентификаторы процессов. Контейнеры могут иметь свои собственные PID, начиная с PID 1.
- **Network Namespace**: Изолирует сетевые интерфейсы, маршруты и порты. Каждый контейнер может иметь свой IP и сетевые интерфейсы.
- **Mount Namespace**: Изолирует файловые системы. Контейнеры могут монтировать свои устройства и файлы, не влияя на хост.
- **UTS Namespace**: Изолирует системные идентификаторы, такие как имя хоста и домен.
- **IPC Namespace**: Изолирует механизмы межпроцессных коммуникаций, такие как очереди сообщений и семафоры.
- **User Namespace**: Изолирует пользователей и группы, что позволяет запускать контейнеры с разными правами пользователей, не влияя на хостовую систему.

### Пример:

Каждый контейнер будет работать в своем собственном **PID** пространстве, т.е. процесс с PID 1 внутри контейнера будет независим от PID 1 на хосте.

## 2. 🔧 **Cgroups (Control Groups)**

**Cgroups** управляют и ограничивают использование ресурсов для контейнеров. Это включает управление CPU, памятью, дисковым пространством и сетевыми ресурсами.

### Как работают Cgroups:

- **Ограничение ресурсов**: Cgroups позволяют ограничивать использование CPU, памяти, сети и других ресурсов.
- **Приоритеты ресурсов**: Cgroups могут назначать приоритеты для контейнеров, чтобы важные процессы получали больше ресурсов.
- **Мониторинг**: Cgroups позволяют отслеживать, сколько ресурсов использует каждый контейнер в реальном времени.

### Пример:

Если контейнер ограничен 1 ГБ памяти, то процесс в этом контейнере не сможет использовать больше 1 ГБ памяти, даже если на хосте есть больше свободной памяти.

## 3. 📂 **Union Filesystem (Объединенные файловые системы)**

**Union Filesystem** позволяет объединять несколько файловых систем в одну. Это позволяет контейнерам использовать **общую базу** файлов и **модифицировать только изменения** в отдельном слое, оставляя базовую систему неизменной.

### Как работает Union Filesystem:

- **Слои**: Каждый контейнер может использовать свой собственный слой файловой системы, который накладывается на базовую систему.
- **Слияние файлов**: Когда контейнер создает или изменяет файлы, эти изменения записываются в новый слой, а старые файлы остаются в базовом слое.
- **OverlayFS**: Один из популярных примеров UnionFS, используемый для контейнеризации.

### Пример:

Если у вас есть базовый образ контейнера с Ubuntu, то все изменения (например, установка новых пакетов) будут записываться в новый слой, а базовый слой останется неизменным.

## 4. 📌 **Как эти технологии работают вместе?**

- **Namespaces** изолируют ресурсы контейнера от хостовой системы и других контейнеров.
- **Cgroups** управляют и ограничивают ресурсы, выделяемые контейнерам.
- **Union Filesystem** позволяет контейнерам эффективно использовать слои файловых систем, минимизируя использование пространства.

Эти технологии в совокупности позволяют контейнерам работать как изолированные единицы, эффективно используя ресурсы хоста и минимизируя накладные расходы на виртуализацию.

## 🧑‍💼 **Итог:**

- **Namespaces** — изоляция процессов, сети и других ресурсов.
- **Cgroups** — управление и ограничение использования ресурсов.
- **Union Filesystem** — эффективное использование файловых систем с несколькими слоями.

</p></details>

---

## ❓ 6. Отличается ли написание Dockerfile для сервиса на компилируемом языке от сервиса на языке, не требующем компиляции?

**[📺 Видео-вопрос (57:13)](https://youtu.be/5-MVBKH77tE?t=3434)**

<details><summary><strong>Показать ответ</strong></summary><p>

> Dockerfile для сервисов на компилируемых языках (например, C, C++, Go) и сервисов на языках, не требующих компиляции (например, Python, Node.js), имеет ряд различий. Эти различия связаны с процессом сборки и требованиями к установке зависимостей.

## 1. 🛠️ **Dockerfile для сервиса на компилируемом языке**

Когда вы работаете с компилируемыми языками, Dockerfile должен включать шаги для установки компилятора, сборки приложения и удаления ненужных файлов для уменьшения размера образа.

### Пример Dockerfile для сервиса на языке C:

```Dockerfile
# Используем официальный образ с компилятором C
FROM gcc:latest

# Устанавливаем рабочую директорию
WORKDIR /app

# Копируем исходный код в контейнер
COPY . .

# Компилируем приложение
RUN gcc -o myapp main.c

# Указываем команду для запуска приложения
CMD ["./myapp"]
```

#### Объяснение:

- **FROM gcc:latest**: используем образ с компилятором GCC.
- **WORKDIR /app**: создаем рабочую директорию.
- **COPY . .**: копируем исходный код.
- **RUN gcc -o myapp main.c**: компилируем приложение.
- **CMD ["./myapp"]**: указываем команду для запуска.

## 2. 🐍 **Dockerfile для сервиса на языке, не требующем компиляции**

Для языков, не требующих компиляции, Dockerfile будет проще. Он включает только шаги для установки зависимостей и запуска приложения.

### Пример Dockerfile для сервиса на Python:

```Dockerfile
# Используем официальный образ с Python
FROM python:3.9-slim

# Устанавливаем рабочую директорию
WORKDIR /app

# Копируем файл зависимостей
COPY requirements.txt .

# Устанавливаем зависимости
RUN pip install --no-cache-dir -r requirements.txt

# Копируем исходный код
COPY . .

# Указываем команду для запуска приложения
CMD ["python", "app.py"]
```

#### Объяснение:

- **FROM python:3.9-slim**: используем образ с Python.
- **WORKDIR /app**: создаем рабочую директорию.
- **COPY requirements.txt .**: копируем файл зависимостей.
- **RUN pip install --no-cache-dir -r requirements.txt**: устанавливаем зависимости.
- **COPY . .**: копируем исходный код.
- **CMD ["python", "app.py"]**: указываем команду для запуска.

## 3. ⚙️ **Основные отличия Dockerfile для компилируемых и не компилируемых языков:**

| Характеристика      | Компилируемый язык (C, Go)                        | Язык без компиляции (Python, Node.js)                         |
| ------------------- | ------------------------------------------------- | ------------------------------------------------------------- |
| **Базовый образ**   | Обычно с компилятором (например, `gcc`, `golang`) | Образы для выполнения (например, `python`, `node`)            |
| **Шаги сборки**     | Включает компиляцию исходного кода                | Обычно установка зависимостей через менеджер пакетов          |
| **Зависимости**     | Компилятор, библиотеки для сборки                 | Менеджер пакетов (например, `pip`, `npm`)                     |
| **Размер образа**   | Как правило, больше из-за наличия компилятора     | Меньше, так как только необходимые библиотеки и интерпретатор |
| **Процесс запуска** | После компиляции — запуск скомпилированного файла | Прямой запуск интерпретируемого файла                         |

## 4. 📌 **Итог:**

- Для **компилируемых языков** (например, C, Go) Dockerfile будет включать этапы для компиляции, что требует дополнительных зависимостей и может увеличить размер образа.
- Для **не компилируемых языков** (например, Python, Node.js) Dockerfile будет проще, так как процесс сводится к установке зависимостей и запуску интерпретируемого кода.

</p></details>

---

## ❓ 7. Какой пользователь будет использоваться по умолчанию при запуске контейнера с PostgreSQL?

**[📺 Видео-вопрос (59:35)](https://youtu.be/5-MVBKH77tE?t=3575)**

<details><summary><strong>Показать ответ</strong></summary><p>

> Когда вы запускаете контейнер с **PostgreSQL** с использованием официального Docker-образа, по умолчанию используется **пользователь `postgres`**.

## 🔑 **Пользователь `postgres` по умолчанию:**

- **`postgres`** — это стандартный пользователь, который создается в контейнере и имеет все привилегии для работы с PostgreSQL.
- Этот пользователь используется для запуска процесса базы данных PostgreSQL и выполнения всех необходимых операций с базой данных.

## ⚙️ **Как проверить пользователя внутри контейнера:**

1. Запустите контейнер с PostgreSQL.
2. Подключитесь к контейнеру:

   ```bash
   docker exec -it some-postgres bash
   ```

3. После подключения выполните команду:

   ```bash
   whoami
   ```

   Ответ будет: **`postgres`**, так как это пользователь по умолчанию для работы с PostgreSQL в контейнере.

## 💡 **Как изменить пользователя по умолчанию:**

Если вы хотите использовать другого пользователя в контейнере, можно указать переменную окружения `POSTGRES_USER`, чтобы создать нового пользователя базы данных.

### Пример:

```bash
docker run --name some-postgres -e POSTGRES_USER=myuser -e POSTGRES_PASSWORD=mypassword -d postgres
```

В этом случае контейнер будет работать с пользователем **`myuser`** вместо **`postgres`**.

## 📌 **Итог:**

- По умолчанию при запуске контейнера с PostgreSQL используется пользователь **`postgres`**.
- Для изменения пользователя базы данных можно использовать переменную окружения **`POSTGRES_USER`**.
- Это позволяет легко настроить пользователя и пароль для базы данных при создании контейнера.

</p></details>

---

## ❓ 8. Какой командой можно посмотреть запущенные процессы на сервере?

**[📺 Видео-вопрос (01:01:42)](https://youtu.be/5-MVBKH77tE?t=3702)**

<details><summary><strong>Показать ответ</strong></summary><p>

В Linux существует несколько команд для просмотра процессов, запущенных на сервере. Каждая команда имеет свои особенности и предоставляет разную информацию о процессах и их ресурсах. Рассмотрим самые популярные команды.

---

## 1. 🔍 **`ps` (Process Status)**

Команда **`ps`** отображает список текущих процессов в системе. Она используется для получения информации о запущенных процессах.

### Пример команды:

```bash
ps aux
```

- **`a`** — отображает процессы всех пользователей.
- **`u`** — показывает процессы с дополнительной информацией (пользователь, потребление ресурсов).
- **`x`** — показывает процессы, не привязанные к терминалу.

### Пример вывода:

```bash
USER       PID  %CPU %MEM    VSZ   RSS TTY      STAT START   TIME COMMAND
root       1001  0.0  0.1  42628  1324 ?        Ss   10:05   0:00 /bin/bash
postgres   1050  2.5  1.2 162400 13768 ?        S    10:20   0:10 postgres: writer process
```

## 3. 🌳 **`pstree`**

Команда **`pstree`** отображает процессы в виде дерева, показывая иерархию процессов. Это полезно для визуализации зависимостей между процессами.

### Пример команды:

```bash
pstree
```

### Пример вывода:

```bash
systemd─┬─apache2─┬─apache2─┬─httpd
        │         │         └─httpd
        │         └─apache2─┬─httpd
        │                   └─httpd
        └─sshd─┬─sshd───bash
                └─sshd───bash
```

---

## 4. 🔎 **`pgrep`**

Команда **`pgrep`** используется для поиска процессов по имени.

### Пример команды:

```bash
pgrep nginx
```

### Пример вывода:

```bash
1024
1032
1040
```

- Выводит список PID процессов, соответствующих имени, например, `nginx`.

## 5. 📂 **`lsof`** (List Open Files)

Команда **`lsof`** отображает список открытых файлов и процессов, которые их используют.

### Пример команды:

```bash
lsof
```

### Пример вывода:

```bash
COMMAND    PID   USER   FD   TYPE DEVICE SIZE/OFF NODE NAME
bash       1234  user   cwd  DIR  8,1     4096   123 /home/user
nginx      5678  root   4u   REG  8,1     2048   456 /var/log/nginx/access.log
```

- Показывает, какие файлы открыты процессами и другие ресурсы, которые используются.

## 📌 **Итог:**

Для просмотра запущенных процессов на сервере в Linux используйте следующие команды:

- **`ps aux`** — статичный список всех процессов.
- **`pstree`** — отображает процессы в виде дерева.
- **`pgrep`** — ищет процессы по имени.
- **`lsof`** — показывает открытые файлы и процессы, их использующие.

</p></details>

---

## ❓ 9. Запускаем контейнер с сервисом в докере дорустим hellow world сможем ли мы увидеть эту команду через ps на хосте?

**[📺 Видео-вопрос (01:02:02)](https://youtu.be/5-MVBKH77tE?t=3722)**

<details><summary><strong>Показать ответ</strong></summary><p>

> Когда вы запускаете контейнер с сервисом, например, с командой `Hello World`, процесс будет запущен **внутри контейнера**, а не на хостовой машине. Однако, в зависимости от контекста, вы сможете увидеть или не увидеть этот процесс с помощью команды **`ps`** на хосте.

## 1. 🌐 **Запуск процесса внутри контейнера**

Если вы запускаете контейнер с командой, например:

```bash
docker run hello-world
```

- Этот процесс будет выполняться **в контейнере** и не будет отображаться в списке процессов на хостовой системе, если вы используете команду **`ps`** на хосте. Контейнер работает в своей **изолированной среде**, и процессы из контейнера не видны с хоста.

## 2. 🔍 **Что видит команда `ps` на хосте?**

- **`ps` на хосте** не покажет процессы внутри контейнера, потому что контейнеры используют **PID namespace**, который изолирует процессы от хостовой системы.
- Процессы, запущенные в контейнере, видны только в **контейнере**, а не на хосте.

### Пример:

```bash
ps aux
```

- Вы увидите процессы на хосте, но не увидите процессы, запущенные в контейнере, например, `Hello World`.

## 3. 🧑‍💻 **Как увидеть процессы внутри контейнера?**

Чтобы увидеть процессы, работающие внутри контейнера, используйте команду:

```bash
docker exec -it <container_id> ps aux
```

- Это позволит вам увидеть процессы, работающие в контейнере, включая ваш процесс `Hello World`.

## 4. 📋 **Как увидеть процесс контейнера на хосте?**

Если вы хотите увидеть процессы, связанные с контейнером, на хостовой машине, используйте команду:

```bash
docker top <container_id>
```

Эта команда покажет процессы внутри контейнера, но с использованием **PID хостовой системы**.

## 5. 🛠️ **Пример работы с контейнером:**

1. Запустите контейнер с сервисом:

   ```bash
   docker run -d --name my_hello_world hello-world
   ```

2. Проверьте процессы на хосте:

   ```bash
   ps aux
   ```

   Вы не увидите **`hello-world`** процесс, потому что он изолирован внутри контейнера.

3. Проверьте процессы внутри контейнера:

   ```bash
   docker exec -it my_hello_world ps aux
   ```

   Вы увидите, что в контейнере выполняется процесс `hello-world`.

## 📌 **Итог:**

- Процесс, запущенный внутри контейнера, **не будет отображаться** в списке процессов на хосте с помощью команды `ps`.
- Чтобы увидеть процессы внутри контейнера, используйте команду **`docker exec -it <container_id> ps aux`**.
- Для просмотра процессов контейнера на хосте используйте команду **`docker top <container_id>`**.

</p></details>

---
