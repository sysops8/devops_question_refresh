<div align="center">
<a href="https://devops-factory.com" >
  <img src="https://devops-factory.com/images/logogit.png" width="250" alt="DevOps Factory">
</a>
</div>
<div align="center">
    <a style="vertical-align: middle;display: inline-block;">
      <img align="center" src="https://readme-typing-svg.demolab.com?font=Fira+Code&size=14&duration=2000&pause=&color=3DD10D&center=true&vCenter=true&multiline=true&width=435&lines=%D0%A1%D0%B4%D0%B5%D0%BB%D0%B0%D0%BD%D0%BE+%D0%B0%D0%BA%D1%82%D0%B8%D0%B2%D0%B8%D1%81%D1%82%D0%BE%D0%BC+%D0%B8%D0%B7+%D0%BA%D0%BB%D1%83%D0%B1%D0%B0+%D0%BF%D1%80%D0%B8+%D1%84%D0%B0%D0%B1%D1%80%D0%B8%D0%BA%D0%B5;By+Dmitriy+Shmakov" alt="" />
    </a>  
  </a>
</div>

<div align="center">
 
  [![DevOps Factory](https://img.shields.io/badge/DevOps_Factory-green?style=for-the-badge)](https://devops-factory.com)
 
  [![YouTube](https://img.shields.io/badge/YouTube-red?style=for-the-badge)](https://youtube.com/@devopsfactory)
  [![Telegram](https://img.shields.io/badge/Telegram-blue?style=for-the-badge)](https://t.me/devops_factory)
</div>

# DevOps Interview Knowledge Base

Разбор **[мок собеса №6](https://www.youtube.com/watch?v=r0OD5Ncvce0&t)** в формате "Вопрос — Ответ" для подготовки к собеседованиям на позицию DevOps.

## 📌 Содержание

- [1. AWS](#1-AWS)
- [2. Linux](#2-linux)
- [3. Docker](#3-docker)

---

# 1. AWS

### ❓ 1. Чем IAM роль отличается от IAM политики?

**[📺 Видео-вопрос (10:45)](https://youtu.be/r0OD5Ncvce0?t=643)**

<details><summary><strong>Показать ответ</strong></summary><p>

- В **AWS IAM (Identity and Access Management)** существуют два основных компонента для управления доступом: **IAM Роль (IAM Role)** и **IAM Политика (IAM Policy)**. Несмотря на то, что эти компоненты тесно связаны, они выполняют разные функции.

## 1. IAM Роль (IAM Role) 🎭

- **Определение**: Роль IAM — это **набор разрешений**, который может быть **принят** (assumed) пользователем, сервисом или ресурсом AWS.
- **Основные особенности**:
  - Роли **не привязаны** к конкретным пользователям.
  - Используются для **предоставления доступа** другим сущностям (например, EC2-инстансам, Lambda-функциям).
  - Могут быть переданы внешним аккаунтам или сервисам, например, для межаккаунтного доступа или делегирования.

### Пример:

Вы создаете роль для **EC2-инстанса**, чтобы он мог получить доступ к **S3**.

## 2. IAM Политика (IAM Policy) 📜

- **Определение**: Политика IAM — это **документ в формате JSON**, который определяет **разрешения** на доступ к ресурсам.
- **Основные особенности**:
  - Политики **определяют**, что именно можно делать с ресурсами (например, чтение, запись).
  - Могут быть привязаны к **пользователям, группам или ролям**.
  - Политики бывают **инлайн** (встроенные) и **управляемые** (прикрепляются к нескольким сущностям).

### Пример:

Вы прикрепляете политику к роли EC2-инстанса, разрешающую доступ к бакетам S3, например, с действиями `s3:ListBucket` и `s3:GetObject`.

## 🔑 Ключевые различия

| **Компонент**            | **IAM Роль (Role)**                                                            | **IAM Политика (Policy)**                                            |
| ------------------------ | ------------------------------------------------------------------------------ | -------------------------------------------------------------------- |
| **Определение**          | Контейнер для разрешений, который может быть принят пользователем или сервисом | Документ, который **определяет разрешения** для действий с ресурсами |
| **Что описывает**        | **Кто** может делать что-то (пользователь, сервис)                             | **Что** разрешено или запрещено с ресурсами                          |
| **Пример использования** | Назначение прав сервисам (например, EC2, Lambda)                               | Определение прав доступа (например, к S3)                            |

---

## 🤝 Как работают вместе?

1. **IAM Роль**: Это **контейнер**, в котором хранятся разрешения.
2. **IAM Политика**: Это **документ**, который прописывает **разрешения** на доступ к ресурсам.

### Пример:

- **Роль**: EC2-инстанс получает роль для доступа к S3.
- **Политика**: Роль прикрепляется к политике, которая разрешает доступ к бакетам S3.

## 🔍 Резюме

- **IAM Роль** — это **пользовательский контейнер**, который **используется сервисами или пользователями** для доступа к ресурсам.
- **IAM Политика** — это **документ** с правилами, который **определяет, какие действия можно или нельзя выполнять** с ресурсами AWS.

💡 Надеюсь, это поможет вам лучше понять, как работает **IAM** в AWS и как использовать **роли** и **политики** для эффективного управления доступом!

</p></details>

---

### ❓ 2. Когда в AWS используется авторизация без двухфакторной аутентификации, для доступа к консоли необходимы AWS Access Key и AWS Secret Key. Как происходит процесс авторизации с двухфакторной аутентификацией и что добавляется к этим ключам?

**[📺 Видео-вопрос (12:35)](https://youtu.be/r0OD5Ncvce0?t=755)**

<details><summary><strong>Показать ответ</strong></summary><p>

# Авторизация в AWS с двухфакторной аутентификацией (MFA) 🔐

Когда в AWS используется двухфакторная аутентификация (MFA), процесс авторизации становится более безопасным. Давайте разберемся, как происходит авторизация с MFA и что добавляется к стандартным ключам доступа.

## 1. **Без двухфакторной аутентификации** 🔑

- Для доступа к AWS без двухфакторной аутентификации достаточно использовать **AWS Access Key** и **AWS Secret Key**.
- Эти ключи используются для аутентификации через API, CLI и другие инструменты.

## 2. **С двухфакторной аутентификацией (MFA)** 🛡️

При использовании **MFA**, процесс авторизации добавляет еще один шаг — проверку через **MFA-токен**. Это повышает уровень безопасности, так как для входа потребуется не только доступ через ключи, но и дополнительный код.

### Шаги авторизации с MFA:

1. **Основные ключи**:

   - Все так же, как и в случае без MFA, вам нужны **AWS Access Key** и **AWS Secret Key** для первого этапа аутентификации через API или CLI.

2. **MFA-устройство**:
   - Для дополнительной защиты требуется **MFA-устройство**, которое генерирует одноразовые пароли (например, **Google Authenticator** или аппаратный токен).
3. **Дополнительный шаг**:

   - При попытке выполнить операцию, требующую MFA, вам нужно будет ввести **временный одноразовый код**, сгенерированный вашим MFA-устройством.

4. **Как это добавляется**:
   - При аутентификации через **AWS Console** или API, помимо Access и Secret Key, вам необходимо ввести одноразовый код MFA.
   - При использовании **AWS CLI** или SDK, MFA-токен передается в запросах с параметрами:
     - `--serial-number` — для устройства MFA.
     - `--token-code` — для одноразового кода, полученного с устройства.

## 3. **Резюме** 📝

- **Без MFA**: Достаточно **AWS Access Key** и **AWS Secret Key** для аутентификации.
- **С MFA**: Помимо ключей доступа, необходимо ввести **временный одноразовый код** с MFA-устройства для выполнения действий, требующих дополнительной безопасности.

🔐 **Заключение**:
Использование **двухфакторной аутентификации** (MFA) значительно повышает безопасность ваших данных в AWS, добавляя дополнительный шаг для подтверждения вашей личности через временные коды.

</p></details>

---

### ❓ 3. Чем отличается gp2 volume от gp3 volume?

**[📺 Видео-вопрос (13:53)](https://youtu.be/r0OD5Ncvce0?t=833)**

<details><summary><strong>Показать ответ</strong></summary><p>

В **Amazon Elastic Block Store (EBS)** существуют различные типы томов для хранения данных, среди которых **gp2** и **gp3** — это два типа **General Purpose SSD (SSD)** томов. Оба типа используются для повседневных рабочих нагрузок, но имеют ряд отличий.

## 1. **gp2 Volume** 📦

**gp2** — это старый тип томов, который использовался в AWS для общих целей. Он обеспечивает хорошую производительность и подходит для большинства рабочих нагрузок.

- **Производительность**:
  - Имеет **базовую производительность** в 3 IOPS на гигабайт (например, для 100 ГБ объема это будет 300 IOPS).
  - Производительность увеличивается пропорционально размеру тома, но существует предел в **16,000 IOPS** и **250 MB/s**.
- **Стоимость**:
  - Стоимость **gp2** томов основана на объеме, который вы используете, и размере тома влияет на максимальное количество IOPS.
- **Основные особенности**:
  - Стандартный тип для большинства рабочих нагрузок.
  - Хорош для баз данных, разработки и тестирования приложений.

## 2. **gp3 Volume** 🚀

**gp3** — это более новый и усовершенствованный тип томов, который был введен AWS, чтобы предоставить **большую гибкость** и улучшенную производительность по сравнению с **gp2**.

- **Производительность**:
  - **gp3** предлагает **постоянные IOPS и пропускную способность**, независимо от размера тома.
  - **IOPS** на **gp3** можно настроить в пределах от **3,000 до 16,000 IOPS**, и это можно сделать независимо от объема.
  - Пропускная способность **gp3** может достигать **1,000 MB/s**.
- **Стоимость**:

  - **gp3** дешевле по сравнению с **gp2** при аналогичных характеристиках.
  - **gp3** позволяет отдельно платить за IOPS и пропускную способность, что дает больше контроля и гибкости в настройке.

- **Основные особенности**:
  - Большая гибкость в настройках IOPS и пропускной способности.
  - Идеален для приложений с интенсивными операциями ввода-вывода (например, базы данных, аналитика, кэширование).
  - **gp3** позволяет снизить затраты, не теряя в производительности.

## 3. **Сравнение: gp2 vs gp3** 🔍

| **Параметр**                     | **gp2**                            | **gp3**                                       |
| -------------------------------- | ---------------------------------- | --------------------------------------------- |
| **Производительность**           | 3 IOPS/ГБ, до 16,000 IOPS          | От 3,000 до 16,000 IOPS                       |
| **Макс. Пропускная способность** | 250 MB/s                           | 1,000 MB/s                                    |
| **Стоимость**                    | Дороже при высокой нагрузке        | Дешевле при аналогичных характеристиках       |
| **Настройка IOPS**               | Автоматическая, зависит от размера | Можно настраивать отдельно от размера         |
| **Оптимальные сценарии**         | Общие приложения, тестирование     | Высокопроизводительные базы данных, аналитика |

## 4. **Заключение** 📝

- **gp2** — это более старый тип томов, который предоставляет базовую производительность в зависимости от размера тома.
- **gp3** — это более гибкий и дешевый тип томов с возможностью настройки производительности, который является лучшим выбором для высоконагруженных приложений и тех, кто хочет контролировать расходы.

Если вам нужна большая гибкость и возможность точной настройки IOPS и пропускной способности, **gp3** — лучший выбор! 💡

</p></details>

---

### ❓ 4. Какие типы instance есть?

**[📺 Видео-вопрос (14:19)](https://youtu.be/r0OD5Ncvce0?t=859)**

<details><summary><strong>Показать ответ</strong></summary><p>

# Типы экземпляров (Instance Types) в AWS EC2 🖥️

# Типы EC2 Инстансов в AWS 🖥️

- AWS EC2 (Elastic Compute Cloud) предлагает широкий выбор типов инстансов для различных рабочих нагрузок. Каждый тип инстанса оптимизирован для конкретных целей, таких как вычисления, память, хранилище или сетевые операции.

Серия **T** — это инстансы с **динамической производительностью** (burstable performance), которые идеально подходят для рабочих нагрузок с переменной нагрузкой.

#### **t1.micro** (устаревший) 🚫

- **Цель**: Для базовых приложений и небольших нагрузок.
- **Процессоры**: Экономичный процессор, подходящий для низкоинтенсивных задач.
- **Характеристики**:
  - 1 vCPU.
  - 0.613 GiB памяти.
  - **Не рекомендуется к использованию**, так как этот тип инстанса был заменен более новыми вариантами.

#### **t2.micro, t2.small, t2.medium** 📈

- **Цель**: Для рабочих нагрузок с переменной или случайной нагрузкой.
- **Процессоры**: Используют **базовую производительность** с возможностью **пикового использования** (burstable).
- **Характеристики**:
  - **t2.micro**: 1 vCPU, 1 GiB памяти.
  - **t2.small**: 1 vCPU, 2 GiB памяти.
  - **t2.medium**: 2 vCPU, 4 GiB памяти.
  - **Технология T2 CPU Credit**: Токены кредитов, которые накапливаются, когда инстанс не работает на полной мощности, и могут быть использованы для кратковременных пиков производительности.

#### **t3.micro, t3.small, t3.medium** 🚀

- **Цель**: Для большинства приложений с переменной нагрузкой.
- **Процессоры**: Более мощные, чем в t2, с поддержкой **CPU Credits**.
- **Характеристики**:
  - **t3.micro**: 2 vCPU, 1 GiB памяти.
  - **t3.small**: 2 vCPU, 2 GiB памяти.
  - **t3.medium**: 2 vCPU, 4 GiB памяти.
  - **Поддержка EC2 Unlimited**: Возможность увеличения производительности за пределы стандартных кредитов, если инстанс не использует все свои кредиты.

### 2. **Типы инстансов серии M** ⚙️

**M**-серия — это инстансы общего назначения с хорошим балансом **в вычислительной мощности**, **памяти** и **сети**.

#### **m3.medium, m3.large** (устаревшие) ❌

- **Цель**: Подходят для обычных рабочих нагрузок.
- **Процессоры**: Стандартная производительность для приложений среднего уровня.

#### **m5.large, m5.xlarge, m5.2xlarge** ✅

- **Цель**: Подходит для балансированных приложений, требующих постоянных вычислительных мощностей.
- **Процессоры**: Современные процессоры Intel Xeon с поддержкой **AVX-512** и оптимизацией для облачных вычислений.
- **Характеристики**:
  - **m5.large**: 2 vCPU, 8 GiB памяти.
  - **m5.xlarge**: 4 vCPU, 16 GiB памяти.

### 3. **Типы инстансов серии C** ⚡

**C**-серия — это инстансы, ориентированные на **высокую вычислительную мощность** и оптимизированы для вычислительно-интенсивных задач.

#### **c4.large, c4.xlarge** (устаревшие) ❌

- **Цель**: Для приложений, требующих высокой производительности процессора.

#### **c5.large, c5.xlarge, c5.2xlarge** 💪

- **Цель**: Высокопроизводительные вычисления, такие как обработка данных, аналитика.
- **Процессоры**: Intel Xeon Scalable с поддержкой **AVX2** и **Turbo Boost**.
- **Характеристики**:
  - **c5.large**: 2 vCPU, 4 GiB памяти.
  - **c5.xlarge**: 4 vCPU, 8 GiB памяти.

### 4. **Типы инстансов серии R** 🧠

**R**-серия — это инстансы, оптимизированные для задач, требующих большой **памяти**.

#### **r5.large, r5.xlarge, r5.2xlarge** 🧳

- **Цель**: Для обработки задач с большим объемом данных в памяти, таких как базы данных и аналитика.
- **Характеристики**:
  - **r5.large**: 2 vCPU, 16 GiB памяти.
  - **r5.xlarge**: 4 vCPU, 32 GiB памяти.

### 5. **Типы инстансов серии P** ⚙️

**P**-серия предназначена для задач, требующих **высоких вычислений с GPU**. Эти инстансы идеальны для **машинного обучения** и **обработки видео**.

#### **p2.xlarge, p2.8xlarge** 🎥

- **Цель**: Для машинного обучения, глубокого обучения и научных вычислений.
- **Процессоры**: Включает **GPU** на базе **NVIDIA Tesla K80**.

### 6. **Типы инстансов серии X** 💾

**X**-серия инстансов предназначена для **высокопроизводительных вычислений с очень большой памятью**.

#### **x1.16xlarge, x1.32xlarge** 🧠

- **Цель**: Для работы с большими базами данных и аналитическими приложениями.
- **Процессоры**: Intel Xeon с большим объемом памяти, оптимизированный для базы данных.

## 7. **Таблица: Сравнение типов инстансов** 🔍

| **Тип инстанса** | **Цель**                          | **Характеристики**                                  |
| ---------------- | --------------------------------- | --------------------------------------------------- |
| **T2 / T3**      | Переменные нагрузки               | Burstable Performance, CPU Credits                  |
| **M5**           | Общие приложения                  | Баланс вычислений, памяти и сети                    |
| **C5**           | Высокая вычислительная мощность   | Оптимизированы для вычислительно-интенсивных задач  |
| **R5**           | Память-ориентированные задачи     | Оптимизированы для обработки больших объемов данных |
| **P2**           | GPU-вычисления, машинное обучение | Для глубокого обучения и научных вычислений         |
| **X1**           | Большая память и вычисления       | Для аналитики и работы с большими данными           |

## 8. **Заключение** 📝

- **Типы T** (например, **t3**) идеальны для малых и средних рабочих нагрузок с переменной производительностью.
- **Типы M** предназначены для рабочих нагрузок общего назначения, где важен баланс между вычислениями, памятью и сетью.
- **Типы C и R** оптимальны для интенсивных вычислений и обработки больших объемов данных соответственно.
- **Типы P и X** используются для задач, требующих **вычислений с GPU** и обработки данных с высокой нагрузкой.

Каждый тип инстанса AWS имеет свои **особенности и применения**, которые помогают оптимизировать расходы и производительность для вашего бизнеса! 🚀

</p></details>

---

### ❓ 5. Почему инстанс t2.micro в AWS начинает сильно тормозить при запуске ресурсоемкого приложения, и что может вызывать такие задержки?

**[📺 Видео-вопрос (15:27)](https://youtu.be/r0OD5Ncvce0?t=927)**

<details><summary><strong>Показать ответ</strong></summary><p>

# Почему инстанс **t2.micro** в AWS начинает тормозить при запуске ресурсоемкого приложения? 🤔

Когда на инстансе **t2.micro** в AWS запускается ресурсоемкое приложение, могут возникать **замедления** и **тормоза**. Это связано с несколькими ключевыми факторами, которые могут влиять на производительность:

## 1. **Ограничения на производительность CPU (CPU Credits)** ⚡

- **t2.micro** использует систему **CPU Credits**, которая позволяет инстансу работать с базовой производительностью.
- Когда инстанс работает с низкой нагрузкой, он накапливает **кредиты производительности**. Однако, при запуске ресурсоемкого приложения, которое требует значительных вычислительных мощностей, кредиты могут быстро исчерпаться.
- После этого инстанс переходит на **базовую производительность**, что вызывает **замедление** работы и снижение скорости.

### Как это влияет:

- При исчерпании кредитов производительность инстанса ограничивается, и приложения начинают работать медленно.

## 2. **Ограничения по памяти (RAM)** 🧠

- **t2.micro** имеет всего **1 GiB** оперативной памяти, что является довольно ограниченным объемом для ресурсоемких приложений.
- Когда приложение требует больше памяти, чем доступно, система может начать использовать **виртуальную память (swap)**, что значительно замедляет производительность.

### Как это влияет:

- Недостаток памяти вызывает **тормоза** при работе приложений, которые нуждаются в большем объеме памяти.

## 3. **Недостаточная мощность для интенсивных задач** 🔋

- **t2.micro** предназначен для **легких рабочих нагрузок**, таких как тестирование или небольшие веб-приложения.
- Ресурсоемкие приложения, например, базы данных или задачи по обработке данных, требуют большего объема **вычислительных мощностей** и **памяти**.

### Как это влияет:

- Инстанс **не справляется с нагрузкой** и начинает сильно тормозить.

## 4. **Превышение квот для входных/выходных операций (I/O)** ⚙️

- Если ваше приложение активно использует **диски** (например, для хранения данных), то на **t2.micro** могут возникать **проблемы с производительностью I/O**.
- Этот инстанс ограничен в возможностях для операций ввода/вывода, что может вызывать задержки.

### Как это влияет:

- При превышении квоты для I/O операций могут возникать **замедления**.

## 5. **Резюме** 📝

- **t2.micro** — это экономичный инстанс, предназначенный для легких нагрузок, и его ресурсы быстро исчерпываются при запуске тяжелых приложений.
- Для ресурсоемких задач лучше использовать инстансы с **большими вычислительными мощностями**, **большим объемом памяти** и возможностью настройки **вычислительных кредитов**.

</p></details>

---

### ❓ 6. Когда используется инстанс серии T в AWS, существует понятие steal time (ST), которое иногда резко возрастает. Что это значит и как это влияет на производительность?

**[📺 Видео-вопрос (17:15)](https://youtu.be/r0OD5Ncvce0?t=1035)**

<details><summary><strong>Показать ответ</strong></summary><p>

- Когда используется инстанс серии **T** в AWS, возникает понятие **Steal Time (ST)**, которое может резко увеличиваться, особенно в случае использования **CPU Credits**. Давайте разберемся, что это значит и как это влияет на производительность.

## 1. **Что такое Steal Time (ST)?** ⏳

**Steal Time (ST)** — это показатель, который измеряет, сколько времени процессор инстанса AWS тратит на выполнение задач других виртуальных машин, работающих на том же физическом сервере (хост-сервере).

- **ST** возрастает, когда **CPU Credits** на вашем инстансе заканчиваются или когда другие инстансы на хост-сервере используют ресурсы процессора интенсивнее, чем ваш инстанс.
- **Steal Time** растет, потому что инстанс больше не может получить достаточно процессорных ресурсов для своих задач и начинает "красть" время процессора у других инстансов.

## 2. **Как Steal Time (ST) связан с CPU Credits?** ⚡

На инстансах серии **T** (например, **t2** и **t3**) AWS использует систему **CPU Credits**, которая позволяет инстансу работать на высокой производительности в течение коротких периодов времени.

- **CPU Credits** накапливаются, когда инстанс работает с низкой нагрузкой, и могут быть использованы для пиковых вычислительных операций.
- Когда инстанс исчерпывает свои кредиты, его производительность ограничивается до **базового уровня**, что может привести к увеличению **Steal Time**.
  - В случае, если другие инстансы на том же физическом сервере требуют больше ресурсов процессора, ваш инстанс может испытывать **конкуренцию за ресурсы**, что приводит к **высокому Steal Time**.

### Как это влияет на производительность?

- Когда **CPU Credits** исчерпаны, инстанс может перейти на базовую производительность, и если другие инстансы на том же хосте используют больше ресурсов, это увеличивает **Steal Time** и вызывает **тормоза**.

## 3. **Как уменьшить Steal Time (ST)?** 🔧

### 1. Перейти на более мощные инстансы:

- Используйте инстансы **C5** или **M5**, которые не зависят от **CPU Credits** и предлагают стабильную производительность для ресурсоемких задач.

### 2. Увеличить **CPU Credits**:

- **T3** инстансы предлагают возможность использовать **EC2 Unlimited**, что позволяет инстансу работать сверх стандартных **CPU Credits**, даже если кредиты накапливаются слишком быстро.

### 3. Масштабирование инстансов:

- Используйте **EC2 Auto Scaling**, чтобы автоматически увеличивать ресурсы в зависимости от нагрузки на ваш инстанс.

## 4. **Резюме** 📝

- **Steal Time (ST)** — это время, когда инстанс теряет доступ к процессорным ресурсам из-за конкуренции с другими инстансами на хост-сервере.
- **ST** напрямую зависит от **CPU Credits**: когда кредиты исчерпываются, производительность инстанса ограничивается, и **Steal Time** может возрастать.
- Чтобы минимизировать **Steal Time**, можно использовать инстансы без зависимости от **CPU Credits** или настроить **EC2 Unlimited** для большего контроля над производительностью.

💡 **Steal Time** — это важный показатель, который можно контролировать, улучшая стабильность и производительность ваших приложений в AWS.

</p></details>

---

### ❓ 7. Какие бывают Load Balancers в AWS?

**[📺 Видео-вопрос (18:43)](https://youtu.be/r0OD5Ncvce0?t=1123)**

<details><summary><strong>Показать ответ</strong></summary><p>

- **Amazon Web Services (AWS)** предоставляет несколько типов **Load Balancer** (балансировщиков нагрузки), которые обеспечивают распределение трафика между вашими **EC2 экземплярами**. Каждый тип балансировщика подходит для различных приложений и сценариев использования. Вот основные типы **Load Balancer** в AWS:

## 1. **Classic Load Balancer (CLB)** ⚖️

### **Определение**:

- **Classic Load Balancer** — это первый тип балансировщика нагрузки в AWS, который работает на **уровне 4 (TCP)** и **уровне 7 (HTTP/HTTPS)** модели OSI.
- CLB идеально подходит для **старых** или **простых приложений**, которые не требуют сложных настроек.

### **Характеристики**:

- **Поддержка TCP и HTTP/HTTPS**: Могут распределять трафик для HTTP(S) и TCP-соединений.
- **Простота**: Быстро настроить, но с ограниченной функциональностью по сравнению с новыми балансировщиками.
- **Сегментация по зонам доступности (AZ)**: Механизм балансировки на основе множества зон доступности для отказоустойчивости.

### **Когда использовать**:

- Для **наследующих** приложений, которые используют старые функции AWS.
- Если вам не нужно использовать продвинутые функции, такие как SSL/TLS оффлоадинг или маршрутизация по содержимому.

## 2. **Application Load Balancer (ALB)** 🖥️

### **Определение**:

- **Application Load Balancer** работает на **уровне 7 (HTTP/HTTPS)** и идеально подходит для **веб-приложений** с гибкой маршрутизацией трафика.
- ALB позволяет маршрутизировать трафик на основе **URL пути**, **заголовков**, **методов HTTP** и других атрибутов.

### **Характеристики**:

- **Маршрутизация по содержимому**: Могут маршрутизировать трафик на основе пути URL, заголовков, методов HTTP, куки и других данных.
- **Поддержка WebSocket**: Может использоваться для взаимодействия в реальном времени.
- **Поддержка контейнеров**: ALB идеально работает с контейнерами в **Amazon ECS** и **EKS**, поддерживая балансировку нагрузки для приложений, работающих внутри контейнеров.
- **SSL/TLS оффлоадинг**: ALB может завершать SSL/TLS-соединение, разгружая ваши сервера от этой задачи.
- **Поддержка HTTP/2**: Улучшенная производительность для веб-приложений.

### **Когда использовать**:

- Для **веб-приложений**, требующих гибкой маршрутизации, например, для микросервисов.
- Когда требуется балансировка трафика на уровне приложений, например, для разбиения по доменам или путям.

## 3. **Network Load Balancer (NLB)** 🌐

### **Определение**:

- **Network Load Balancer** работает на **уровне 4 (TCP/UDP)** и предназначен для **обработки большого объема трафика** и **низкой задержки**.
- NLB идеально подходит для приложений, где важна высокая производительность и способность обрабатывать миллионы запросов в секунду.

### **Характеристики**:

- **Высокая производительность**: Обработка миллионов запросов с минимальной задержкой.
- **Поддержка TCP/UDP**: Идеален для **независимых от приложения** протоколов, таких как TCP или UDP.
- **Поддержка статических IP-адресов**: Вы можете использовать статический IP или Elastic IP.
- **Высокая доступность**: Работает на уровне зон доступности, автоматически распределяя трафик между экземплярами EC2.
- **Поддержка TLS оффлоадинга**: Позволяет завершить TLS-соединение на уровне балансировщика.

### **Когда использовать**:

- Когда требуется **высокая производительность** с минимальной задержкой и **обработка большого объема трафика**.
- Для **мобильных приложений**, **гейминга**, **IoT** и других приложений с требованиями к низкой задержке.

## 4. **Gateway Load Balancer (GLB)** 🛡️

### **Определение**:

- **Gateway Load Balancer** работает на **уровне 3 (IP)** и предназначен для интеграции с сетевыми устройствами **NGFW (Next Generation Firewall)**, **IDS/IPS** и другими сервисами безопасности.
- GLB используется для **маршрутизации трафика через виртуальные appliances**, такие как фаерволы или системы предотвращения вторжений.

### **Характеристики**:

- **Интеграция с виртуальными устройствами**: GLB может направлять трафик к виртуальным фаерволам, системам мониторинга и безопасности.
- **Поддержка масштабируемости**: Автоматическое масштабирование в зависимости от нагрузки.
- **Сетевые правила**: Позволяет фильтровать и проверять трафик через виртуальные appliances.

### **Когда использовать**:

- Для **сетевых решений**, где необходимо интегрировать балансировку нагрузки с системами безопасности, например, для маршрутизации трафика через фаерволы.

## 5. **Чем отличаются типы Load Balancer?** 📊

| Характеристика                 | **Classic Load Balancer**                | **Application Load Balancer (ALB)**              | **Network Load Balancer (NLB)**                      | **Gateway Load Balancer (GLB)**            |
| ------------------------------ | ---------------------------------------- | ------------------------------------------------ | ---------------------------------------------------- | ------------------------------------------ |
| **Уровень модели OSI**         | Уровень 4 (TCP) и уровень 7 (HTTP/HTTPS) | Уровень 7 (HTTP/HTTPS)                           | Уровень 4 (TCP/UDP)                                  | Уровень 3 (IP)                             |
| **Тип трафика**                | TCP, HTTP, HTTPS                         | HTTP, HTTPS, WebSocket, HTTP/2                   | TCP, UDP                                             | IP, для виртуальных устройств              |
| **Гибкость маршрутизации**     | Обычная маршрутизация по IP              | Маршрутизация по содержимому, путям и заголовкам | Основная маршрутизация по IP и протоколам            | Маршрутизация через виртуальные устройства |
| **Поддержка контейнеров**      | Ограниченная                             | Полная поддержка для Amazon ECS и EKS            | Ограниченная                                         | Не поддерживает                            |
| **Высокая производительность** | Средняя                                  | Средняя                                          | Высокая, подходит для приложений с высокой нагрузкой | Средняя                                    |

## Заключение 🎯

AWS предоставляет несколько типов **Load Balancer**, каждый из которых подходит для определенных типов приложений и сценариев. Выбор правильного типа зависит от ваших требований к производительности, гибкости маршрутизации и уровня работы с сетевыми протоколами.

- **Classic Load Balancer** — для старых приложений с базовыми требованиями.
- **Application Load Balancer (ALB)** — для веб-приложений с гибкой маршрутизацией и поддержкой контейнеров.
- **Network Load Balancer (NLB)** — для высокопроизводительных приложений с низкой задержкой.
- **Gateway Load Balancer (GLB)** — для интеграции с виртуальными устройствами безопасности.

</p></details>

---

### ❓ 8. Чем отличается Public IP от Elastic IP?

**[📺 Видео-вопрос (22:45)](https://youtu.be/r0OD5Ncvce0?t=1365)**

<details><summary><strong>Показать ответ</strong></summary><p>

- **Amazon EC2** предоставляет два типа публичных IP-адресов для связи с интернетом: **Public IP** и **Elastic IP (EIP)**. Они оба выполняют схожие функции, но имеют ключевые различия в использовании, стоимости и функциональности.

## 1. **Public IP** 🌍

- **Определение**: **Public IP** — это **временный IP-адрес**, который автоматически присваивается **EC2 экземпляру** при запуске.
- **Назначение**: Используется для обеспечения доступа к экземпляру EC2 через интернет.
- **Основные характеристики**:
  - Присваивается автоматически при запуске экземпляра.
  - **Тиминг**: IP-адрес будет удален при остановке или терминции экземпляра.
  - **Неустойчивый**: Этот IP-адрес меняется каждый раз, когда экземпляр останавливается и запускается снова.
  - **Ограничение**: Не может быть использован для подключения после завершения работы экземпляра.

### Преимущества:

- Простота использования, так как назначается автоматически.
- Быстрая настройка и нет необходимости в дополнительной настройке или управлении.

### Ограничения:

- Временно и может измениться при остановке экземпляра.
- Используется только для связи во время работы экземпляра.

## 2. **Elastic IP (EIP)** 🔒

- **Определение**: **Elastic IP (EIP)** — это **постоянный публичный IP-адрес**, который можно **закрепить за экземпляром EC2** и использовать, даже если экземпляр перезапускается или меняет его состояние.
- **Назначение**: Предназначен для случаев, когда необходимо поддерживать стабильный публичный IP, например, для веб-серверов, которые должны оставаться доступными по одному и тому же IP-адресу.
- **Основные характеристики**:
  - Присваивается вручную через **AWS Management Console** или **AWS CLI**.
  - **Стабильный**: IP-адрес сохраняется при остановке или перезапуске экземпляра.
  - **Может быть переназначен**: Вы можете прикрепить EIP к другому экземпляру EC2 в случае сбоя или изменения инфраструктуры.
  - **Больше контроля**: Может быть использован для динамического изменения маршрута трафика между EC2 экземплярами.

### Преимущества:

- **Стабильность**: IP-адрес не меняется, что идеально подходит для приложений с высокими требованиями к доступности.
- **Переназначение**: Легко переназначить EIP на другой экземпляр в случае сбоя.
- **Гибкость**: Позволяет работать с отказоустойчивыми системами, используя инструменты AWS, такие как **Auto Scaling**.

### Ограничения:

- **Стоимость**: Elastic IP, который не используется (не прикреплен к работающему экземпляру), может быть платным.
- **Ограничение по количеству**: AWS предоставляет один EIP бесплатно при условии, что он используется с запущенным экземпляром. Дополнительные IP-адреса требуют оплаты.

## 3. **Ключевые различия** 📊

| Характеристика                | **Public IP**                                           | **Elastic IP (EIP)**                                                                       |
| ----------------------------- | ------------------------------------------------------- | ------------------------------------------------------------------------------------------ |
| **Назначение**                | Временное, автоматически присваивается при запуске EC2. | Постоянный IP-адрес, закрепляется вручную.                                                 |
| **Сохранность при остановке** | IP-адрес меняется при остановке/перезапуске экземпляра. | IP-адрес сохраняется, можно переназначить.                                                 |
| **Переназначение**            | Невозможно переназначить.                               | Можно переназначить на другой экземпляр.                                                   |
| **Стоимость**                 | Бесплатно при использовании с EC2.                      | Бесплатно, если используется с работающим экземпляром. Дополнительные EIP подлежат оплате. |
| **Использование**             | Подходит для краткосрочных задач или разработки.        | Подходит для долгосрочных и стабильных приложений, которые требуют постоянного IP.         |

## Когда использовать **Elastic IP**:

- Когда требуется **стабильный публичный IP-адрес**, который не меняется, даже если экземпляр перезапускается или останавливается.
- Для **отказоустойчивых** систем, где необходимо быстро перенаправлять трафик на новый экземпляр в случае сбоя.
- Для приложений, которым нужен **постоянный адрес** для DNS записей или интеграции с внешними системами.

## Заключение 🎯

- **Public IP** подходит для временных приложений, в которых не требуется постоянный доступ по одному и тому же IP.
- **Elastic IP** — идеальный выбор, если необходимо использовать стабильный IP-адрес для критичных приложений, требующих высокой доступности.

</p></details>

---

### ❓ 9. Как в S3 можно создать пустую папку?

**[📺 Видео-вопрос (10:45)](https://youtu.be/)**

<details><summary><strong>Показать ответ</strong></summary><p>

В **Amazon S3** нет прямой возможности создать папку, как в традиционной файловой системе, потому что S3 — это **объектное хранилище**, где папки — это всего лишь имена объектов, содержащие слэш (`/`). Однако, создать пустую папку можно с помощью нескольких методов.

## 1. **Создание пустой папки через консоль S3** 🖥️

1. Перейдите в **Amazon S3 Console**.
2. Откройте бакет, в который хотите добавить папку.
3. Нажмите на кнопку **Create folder** (Создать папку).
4. Укажите имя папки (например, `folder_name/`).
5. Нажмите **Create**.

Консоль автоматически создаст объект с нулевым размером в папке, что будет имитировать создание пустой папки.

## 2. **Создание пустой папки с помощью AWS CLI** 💻

Для создания папки с использованием **AWS CLI** можно создать объект с именем, заканчивающимся на слэш (`/`):

```bash
aws s3api put-object --bucket your-bucket-name --key "folder_name/"
```

Этот командный запрос создаст пустой объект с указанным ключом, который будет отображаться как папка в интерфейсе S3.

## 3. **Создание пустой папки с помощью AWS SDK** 📚

Если вы разрабатываете приложение, можно создать пустую папку программно с использованием **AWS SDK** для нужного языка. Пример на Python с использованием `boto3`:

```python
import boto3

# Создаем клиент S3
s3 = boto3.client('s3')

# Указываем имя бакета и ключ для папки
s3.put_object(Bucket='your-bucket-name', Key='folder_name/')
```

Этот код создаст объект с ключом `folder_name/` в вашем бакете, что будет восприниматься как пустая папка.

## 4. **Важно помнить** ⚠️

- В S3 папки на самом деле не существуют, они лишь имитируются через объекты с префиксами.
- Для создания "папки" достаточно создать объект с ключом, заканчивающимся на `/`.
- Папка будет отображаться в интерфейсе S3, но на самом деле это объект с пустым содержимым.

</p></details>

---

### ❓ 10. Есть какие-то ограничения в именовании S3 Bucket?

**[📺 Видео-вопрос (24:31)](https://youtu.be/r0OD5Ncvce0?t=1471)**

<details><summary><strong>Показать ответ</strong></summary><p>

- **Amazon S3** имеет определенные ограничения и правила для именования бакетов, чтобы гарантировать их уникальность и соответствие стандартам безопасности. Вот основные ограничения и рекомендации по именованию бакетов в **S3**.

## 1. **Длина имени бакета** 📏

- Имя бакета должно содержать от **3 до 63 символов**.
- Имя не может быть **пустым**.

## 2. **Допустимые символы** 🅰️

- Имя бакета может содержать **только строчные буквы**, **цифры**, **дефисы (-)** и **точки (.)**.
- **Недопустимые символы**: заглавные буквы, пробелы и другие спецсимволы.

## 3. **Начало и конец имени** 🔠

- Имя бакета **должно начинаться и заканчиваться на букву или цифру**.
- Оно не может начинаться или заканчиваться дефисом **`-`**.

**Правильно**:

```
my-bucket-name
bucket123
```

**Неправильно**:

```
-incorrect-bucket
bucket-name-
```

## 4. **Использование точек (.)** 📍

- Имя бакета может содержать **точки (.)**, но необходимо соблюдать дополнительные правила:
  - Если используется точка, вы не можете использовать дефис рядом с точкой.
  - Точки могут быть использованы для разделения разных частей имени (например, `my.bucket.name`), но это может вызвать проблемы с SSL-сертификатами для HTTPS-соединений.

**Правильно**:

```
my.bucket.name
```

**Неправильно**:

```
my..bucket
```

## 5. **Уникальность имени** 🌍

- Имена бакетов **должны быть уникальными в рамках всего S3** (по всему региону и всем аккаунтам). Это означает, что вы не можете использовать одно и то же имя бакета, которое уже существует в системе AWS.
- Например, если кто-то уже использует бакет с именем `my-bucket-name`, вы не сможете создать свой бакет с таким же именем.

## 6. **Региональные ограничения** 🌎

- Имена бакетов должны соответствовать **региону**, в котором они будут развернуты, и правилам DNS. Это может включать ограничения на использование некоторых символов в имени в зависимости от региона.

## 7. **Рекомендации по именованию** 💡

- **Простота**: Старайтесь выбирать короткие и простые имена для удобства работы.
- **Читаемость**: Используйте дефисы для разделения частей имени, чтобы оно было более читаемым.
- **Уникальность**: Создавайте имена, которые легко идентифицируются и не будут конфликтовать с существующими.
- **Безопасность**: Избегайте использования чувствительных данных в имени бакета (например, персональных данных или паролей).

## 8. **Пример допустимых имен бакетов** ✅

- `my-bucket-2023`
- `mycompany-data-bucket`
- `logs-1234`
- `bucket1`

## **Пример недопустимых имен бакетов** ❌

- `MyBucket` (с заглавными буквами)
- `bucket@name` (с символами, не разрешенными в имени)
- `bucket--name` (с несколькими дефисами подряд)
- `my-bucket_#123` (с некорректными символами)

</p></details>

---

### ❓ 11. Какие типы storage бывают в S3 Bucket?

**[📺 Видео-вопрос (25:10)](https://youtu.be/r0OD5Ncvce0?t=1510)**

<details><summary><strong>Показать ответ</strong></summary><p>

# Типы хранилищ в **S3 Bucket** в AWS 🗄️

- Amazon S3 (Simple Storage Service) предлагает несколько типов хранилищ, которые оптимизированы для разных сценариев использования, таких как стоимость хранения, доступность и скорость. Эти типы хранилищ называются **Storage Classes**. Давайте разберемся, какие они бывают.

## 1. **Standard** (Стандарт) 🌍

**S3 Standard** — это класс хранения для **часто используемых данных** с высокой доступностью и низкой задержкой.

- **Основные характеристики**:
  - Высокая доступность (99.99%).
  - Низкая задержка и высокая производительность.
  - Подходит для активных данных, таких как веб-сайты, мобильные приложения, базы данных.
- **Когда использовать**:
  - Для данных, которые часто запрашиваются или изменяются, например, в **веб-сайтах** и **мобильных приложениях**.

## 2. **Intelligent-Tiering** (Интеллектуальный выбор) 🧠

**S3 Intelligent-Tiering** автоматически перемещает данные между двумя уровнями хранения (часто используемые и редко используемые) в зависимости от паттернов доступа, что позволяет снизить стоимость хранения, не теряя доступности.

- **Основные характеристики**:
  - Автоматическое перемещение объектов между 2 уровнями.
  - **Два уровня**: **Frequent Access** (часто запрашиваемые) и **Infrequent Access** (редко запрашиваемые).
  - Подходит для данных с **непредсказуемыми паттернами доступа**.
- **Когда использовать**:
  - Для данных с переменным или непредсказуемым доступом, где важно автоматически оптимизировать стоимость хранения.

## 3. **Standard-IA (Infrequent Access)** (Реже доступные) 📉

**S3 Standard-IA** предназначен для **редко используемых данных**, которые всё равно требуют высокой доступности.

- **Основные характеристики**:
  - Подходит для данных, которые **редко запрашиваются**, но должны быть доступны быстро, когда это необходимо.
  - Стоимость хранения ниже, чем у **S3 Standard**, но есть **плата за доступ** (чтобы получить данные).
- **Когда использовать**:
  - Для резервных копий, архивации данных, которые редко требуются, но должны быть доступны в случае необходимости.

## 4. **One Zone-IA** (Одинарная зона доступности) ⚠️

**S3 One Zone-IA** — это вариант **Standard-IA**, но с хранением данных в одной зоне доступности, что делает его более экономичным вариантом.

- **Основные характеристики**:
  - Хранение в одной зоне доступности.
  - Подходит для **редко используемых данных**, которые можно восстановить в случае потери зоны.
  - Стоимость хранения ниже, чем у **Standard-IA**, но с некоторыми рисками для доступности данных.
- **Когда использовать**:
  - Для **клонов или копий данных**, где не требуется резервное копирование в нескольких зонах, например, для данных, которые можно восстановить с других источников.

## 5. **Glacier** (Архивное хранилище) 🏔️

**S3 Glacier** — это класс хранения для **долгосрочной архивной хранения данных**, которые требуют **низкой стоимости**.

- **Основные характеристики**:
  - **Архивирование** данных с очень низкой стоимостью хранения.
  - Доступ к данным может занять от **minutes до hours** в зависимости от выбранной скорости восстановления.
- **Когда использовать**:
  - Для долгосрочного хранения данных, таких как **архивы**, **резервные копии** и **журналы**, доступ к которым требуется редко.

## 6. **Glacier Deep Archive** (Глубокий архив) 🏚️

**S3 Glacier Deep Archive** — это наиболее экономичное хранилище для **долгосрочного архивирования**, предназначенное для данных, которые редко запрашиваются.

- **Основные характеристики**:
  - Самая низкая стоимость хранения среди всех классов.
  - Восстановление данных может занять от **12 до 48 часов**.
- **Когда использовать**:
  - Для хранения данных, к которым очень редко требуется доступ, например, **архивы, юридические документы**, которые должны храниться по требованию закона.

## 7. **S3 Outposts** (Сохранение данных на локальных серверах) 🌐

**S3 Outposts** предоставляет возможность хранить данные на локальных серверах в рамках облачной инфраструктуры AWS, при этом используя возможности S3 для управления и доступа.

- **Основные характеристики**:

  - Данные хранятся в **локальных дата-центрах** (на **Outposts**).
  - Использует все возможности S3, включая управление данными и защиту.

- **Когда использовать**:
  - Для ситуаций, когда нужно хранить данные в локальных дата-центрах, но с возможностью интеграции с облачными решениями.

## 8. **Сравнение типов хранилищ в S3** ⚖️

| **Тип**                  | **Описание**                               | **Стоимость хранения** | **Доступность**                             | **Когда использовать**                        |
| ------------------------ | ------------------------------------------ | ---------------------- | ------------------------------------------- | --------------------------------------------- |
| **Standard**             | Для часто используемых данных              | Высокая стоимость      | Высокая доступность                         | Активные данные, веб-приложения               |
| **Intelligent-Tiering**  | Автоматическая оптимизация хранения        | Средняя стоимость      | Высокая доступность                         | Неопределенный или переменный доступ к данным |
| **Standard-IA**          | Для редко используемых данных              | Средняя стоимость      | Высокая доступность                         | Резервное копирование, архивы                 |
| **One Zone-IA**          | Для редко используемых данных в одной зоне | Низкая стоимость       | Высокая доступность                         | Данные, которые можно восстановить            |
| **Glacier**              | Для архивных данных                        | Очень низкая стоимость | Низкая (доступ по запросу)                  | Долгосрочное хранение данных                  |
| **Glacier Deep Archive** | Для долгосрочного архивного хранения       | Наименьшая стоимость   | Очень низкая (доступ через несколько часов) | Очень редкий доступ, архивы                   |
| **S3 Outposts**          | Для локального хранения данных             | Средняя стоимость      | Зависит от локальной инфраструктуры         | Локальное хранение с использованием S3        |

## 9. **Резюме** 📝

- **S3 Standard** — идеален для активных данных.
- **S3 Intelligent-Tiering** автоматически управляет хранением в зависимости от использования.
- **S3 Standard-IA** и **One Zone-IA** подходят для данных, которые редко используются, но должны быть доступны быстро.
- **S3 Glacier** и **Glacier Deep Archive** идеально подходят для архивирования с разными требованиями по скорости восстановления.
- **S3 Outposts** дает возможность хранить данные локально с управлением через S3.

Каждый тип хранилища в **S3** предоставляет баланс между **стоимостью**, **доступностью** и **производительностью**, что позволяет оптимизировать хранение данных в зависимости от ваших потребностей! 🌟

</p></details>

---

### ❓ 12. Как настроить хостинг вебсайта на S3 для обеспечения максимальной скорости загрузки при большом трафике и пользователях из разных регионов?

**[📺 Видео-вопрос (27:13)](https://youtu.be/r0OD5Ncvce0?t=1633)**

<details><summary><strong>Показать ответ</strong></summary><p>

## 1. **Использование Amazon CloudFront (CDN)** 🌍

**CloudFront** — это **сеть доставки контента (CDN)** от AWS, которая кэширует копии вашего контента в различных **глобальных точках присутствия (edge locations)**. Это гарантирует, что пользователи смогут получать данные с сервера, расположенного ближе всего к их физическому местоположению, что значительно ускоряет время загрузки.

- **Как это работает**: Когда пользователь обращается к вашему сайту, CloudFront автоматически перенаправляет его запрос к ближайшему серверу (edge location), который имеет кэшированную копию данных.
- **Преимущества**:
  - Ускоряет загрузку сайта по всему миру.
  - Снижает нагрузку на ваш основной сервер (S3).
  - Обеспечивает более быстрый отклик при большом количестве пользователей.

## 2. **Настройка S3 для высокой доступности и производительности** ⚡

- Используйте **S3 Transfer Acceleration** для ускорения загрузки данных в S3, особенно если у вас есть пользователи, которые загружают файлы на сайт из разных регионов.
- С помощью **S3 Multi-Region Access Points** можно ещё больше улучшить доступность и производительность, автоматически перенаправляя запросы к ближайшему региону.

## 3. **Оптимизация контента** 🖼️

- Используйте **оптимизацию изображений** и других медиафайлов для минимизации их размера, не теряя качества. Это помогает ускорить загрузку страниц.
- Используйте форматы изображений, такие как **WebP**, которые предлагают лучшее сжатие.

## 4. **Настройка HTTPS с помощью Amazon Certificate Manager (ACM)** 🔒

- Для обеспечения безопасности и доверия от пользователей, настройте HTTPS для вашего сайта с помощью **Amazon Certificate Manager (ACM)**. CloudFront позволяет использовать сертификаты SSL/TLS для безопасного соединения.

## 5. **Кэширование и оптимизация через CloudFront** 🧹

- Настройте **CloudFront caching** для увеличения времени жизни кэшированных объектов и уменьшения числа запросов к вашему S3 бакету.
- Установите **TTL** (Time-to-Live) для кэширования контента, чтобы оптимизировать использование ресурсов и уменьшить нагрузку.

## 7. **Пример настройки** 💡

1. Загружаете все статические файлы вашего вебсайта в S3.
2. Включаете **Static Website Hosting** для вашего S3 бакета.
3. Настроите **CloudFront** для доставки контента с низкой задержкой по всему миру.
4. Используете **CloudFront caching** для минимизации запросов к S3.
5. Настроили **SSL/TLS** через **ACM** и обеспечили доступ по HTTPS.

</p></details>

---

### ❓ 13. Если я внес изменения на сайте, будут ли они сразу отображаться через CDN?

**[📺 Видео-вопрос (28:00)](https://youtu.be/r0OD5Ncvce0?t=1680)**

<details><summary><strong>Показать ответ</strong></summary><p>

- Когда вы вносите изменения на сайте, который использует **CDN** (например, **CloudFront** в AWS), изменения **не отображаются мгновенно**. Это связано с тем, что **CDN кэширует контент** в **edge locations** для улучшения производительности и снижения нагрузки на ваш сервер.

## Почему изменения не отображаются сразу? 🤔

- **Кэширование**: Когда вы обновляете сайт, CDN может всё ещё отдавать **кэшированные копии старого контента**. Это происходит, потому что CDN сохраняет статические файлы, такие как изображения, HTML, CSS и JavaScript, для быстрой доставки.
- **Время жизни (TTL)**: У каждого объекта в CDN есть **TTL (Time-to-Live)** — это время, на которое он кэшируется. После этого срока CDN будет запрашивать обновлённую версию с вашего оригинального сервера. Пока TTL не истечёт, изменения не будут видны всем пользователям.

## Что можно сделать, чтобы ускорить отображение изменений? ⚡

### 1. Очистка кэша с помощью CloudFront Invalidation 🧹

#### Шаги для очистки кэша через консоль AWS:

1. Перейдите в **AWS Management Console**.
2. Откройте сервис **CloudFront**.
3. В панели навигации слева выберите **Distributions**.
4. Найдите нужное **Distribution ID**, для которого нужно очистить кэш.
5. Выберите его и перейдите во вкладку **Invalidations**.
6. Нажмите кнопку **Create Invalidation** (Создать инвалидацию).
7. В поле **Object Paths** укажите пути к объектам, которые нужно очистить, например:
   - `/*` — это очистит все объекты в кэше.
8. Нажмите **Invalidate** (Инвалидировать).

#### Пример:

- Если вы хотите очистить кэш для всего контента, используйте путь `/*`.
- Если нужно очистить только определённый файл, например, обновлённый CSS, укажите путь, например, `/styles/*.css`.

### 2. Очистка кэша с помощью AWS CLI 🖥️

Кроме консоли, можно использовать **AWS CLI** для очистки кэша. Для этого выполните команду:

```bash
aws cloudfront create-invalidation --distribution-id <Your_Distribution_ID> --paths "/*"
```

- Замените `<Your_Distribution_ID>` на ваш **Distribution ID**.
- Параметр `/*` очистит весь кэш в вашем распределении CloudFront. Вы можете указать конкретные пути, если хотите очистить кэш только для отдельных объектов.

## 3. Использование версионирования URL 🔑

- Чтобы избежать кэширования старых версий файлов, можно изменять **имена файлов** при каждом обновлении (например, добавлять хеши в имена файлов). Таким образом, CDN воспринимает новый файл как уникальный, и кэшируется он заново.

## Резюме 📝

- Изменения **не всегда будут отображаться сразу** через CDN из-за кэширования.
- Чтобы изменения отображались быстрее, можно **очистить кэш** или уменьшить **TTL** на объекты.
- Также помогает использование **версирования URL**, чтобы каждый файл имел уникальный идентификатор.

🔧 **Инструменты и методы** для быстрой актуализации контента: очистка кэша, уменьшение TTL и версионирование URL.

</p></details>

---

### ❓ 14. Как реализованы бекапы в RDS?

**[📺 Видео-вопрос (29:35)](https://youtu.be/r0OD5Ncvce0?t=1775)**

<details><summary><strong>Показать ответ</strong></summary><p>

- **Amazon RDS (Relational Database Service)** предоставляет несколько механизмов для автоматического и ручного создания **бэкапов** баз данных. Эти функции обеспечивают **высокую доступность**, **защищенность данных** и **восстановление** в случае потери данных.

## 1. **Автоматические бэкапы (Automated Backups)** 🔄

### Что это такое?

- **Автоматические бэкапы** в **Amazon RDS** — это функция, которая позволяет **автоматически** создавать резервные копии базы данных на регулярной основе.
- AWS сохраняет **текущие данные и журналы транзакций** на протяжении определённого времени, чтобы вы могли восстановить базу данных до любого состояния в пределах **периода хранения бэкапов** (обычно от 1 до 35 дней).

### Особенности:

- **Регулярность**: Бэкапы создаются ежедневно (по умолчанию) в **указанный вами временной интервал** (например, с 2 до 3 утра).
- **Ретенция**: Вы можете настроить продолжительность хранения бэкапов от 1 до 35 дней.
- **Полное восстановление**: Вы можете восстановить базу данных до конкретного времени в пределах **хранения журналов транзакций**.
- **Мгновенное восстановление**: Можно быстро восстановить базу данных из автоматических бэкапов или по времени.

### Как работает:

- **Снимки базы данных** (snapshots) выполняются на уровне **хранилища**.
- Бэкапы включают не только **данные**, но и **журналы транзакций** для обеспечения точности восстановления.

## 2. **Ручные бэкапы (Manual Snapshots)** 🛠️

### Что это такое?

- **Ручные бэкапы** — это **снимки базы данных**, которые вы создаёте вручную в любой момент времени.
- В отличие от **автоматических бэкапов**, эти снимки остаются с вами **неограниченно долго** (пока вы их не удалите).

### Особенности:

- **Без срока хранения**: Ручные бэкапы не имеют ограничений по времени и могут храниться на **неопределённый срок**.
- **Восстановление**: Вы можете восстановить базу данных до состояния, которое было зафиксировано в момент создания снимка.
- **Создание вручную**: Для создания снимка необходимо вручную инициировать процесс через консоль, CLI или API.

### Как работает:

- **Ручной снимок** фиксирует текущее состояние базы данных и все её настройки.
- Он не влияет на автоматические бэкапы, и его можно использовать для **восстановления в любой момент**.

## 3. **Бэкапы для Multi-AZ (Высокая доступность)** 🌍

### Что это такое?

- Если база данных настроена на **Multi-AZ** (много зон доступности), то **автоматические бэкапы** выполняются также для **вторичной базы данных** в другой зоне доступности.
- Это гарантирует **высокую доступность** и **устойчивость** базы данных при сбоях в одной из зон доступности.

### Особенности:

- **Автоматические репликации**: Amazon RDS автоматически реплицирует данные на вторичный экземпляр в другом регионе для обеспечения непрерывной доступности.
- **Переключение на резервный экземпляр**: В случае сбоя основной базы данных, RDS автоматически переключится на резервный экземпляр, чтобы обеспечить непрерывную работу.
- **Дополнительное время на восстановление**: Восстановление данных из автоматических бэкапов будет происходить с учётом репликации.

## 4. **Как восстановить данные из бэкапа?** ⏳

### Восстановление из автоматических бэкапов:

1. Перейдите в **Amazon RDS Console**.
2. Выберите базу данных, для которой хотите восстановить бэкап.
3. Нажмите **Restore to point in time**.
4. Укажите дату и время, до которого хотите восстановить базу данных.

### Восстановление из ручного снимка:

1. Перейдите в **RDS Console**.
2. Выберите снимок, с которым хотите работать.
3. Нажмите **Restore Snapshot**.
4. Создайте новый экземпляр базы данных на основе этого снимка.

## 5. **Безопасность и шифрование бэкапов** 🔐

- **Шифрование**: Все бэкапы в **RDS** могут быть зашифрованы с использованием **KMS** (AWS Key Management Service).
- **Сетевые ограничения**: Бэкапы могут быть защищены дополнительными уровнями безопасности, например, через **VPC** или **IAM политики**.

## 6. **Резюме** 📝

- **Автоматические бэкапы** обеспечивают регулярное создание резервных копий и восстановление до конкретного времени.
- **Ручные бэкапы** позволяют сохранять снимки базы данных на неограниченное время и использовать их для восстановления.
- **Multi-AZ** увеличивает доступность и устойчивость системы, автоматически выполняя бэкапы в разных зонах доступности.
- **Шифрование и безопасность** обеспечивают защиту данных как при хранении, так и при восстановлении.

</p></details>

---

### ❓ 15. Какой сервис позволяет создать общую файловую систему между двумя инстансами?

**[📺 Видео-вопрос (30:25)](https://youtu.be/r0OD5Ncvce0?t=1825)**

<details><summary><strong>Показать ответ</strong></summary><p>

- Для создания общей файловой системы между двумя инстансами в AWS используется сервис **Amazon Elastic File System (EFS)**.

## 1. **Что такое Amazon EFS?** 🌐

**Amazon EFS** — это **масштабируемая облачная файловая система**, которая позволяет нескольким **EC2 инстансам** одновременно подключаться к общей файловой системе через стандартный протокол **NFS** (Network File System). Это идеально подходит для сценариев, где необходимо совместное использование данных между несколькими инстансами.

### Основные особенности **EFS**:

- **Подключение через NFS**: EFS использует протокол **NFSv4** для подключения, что позволяет инстансам подключаться и работать с файлами одновременно.
- **Масштабируемость**: EFS автоматически масштабируется в зависимости от объема данных, которые вы храните.
- **Совместное использование**: Несколько EC2 инстансов могут одновременно читать и записывать в файловую систему.
- **Высокая доступность и отказоустойчивость**: EFS автоматически реплицирует данные по нескольким зонам доступности (Availability Zones) в выбранном регионе.

## 2. **Как работает Amazon EFS?** ⚙️

1. **Создание файловой системы** в **EFS** через консоль или командную строку (CLI).
2. **Монтирование файловой системы** на **EC2 инстансы** с использованием протокола NFS.
3. **Работа с файлами**: Все изменения, сделанные одним инстансом, становятся доступны для всех других инстансов, подключённых к этой файловой системе.

## 3. **Пример использования EFS** 📝

1. Создайте **EFS файловую систему** через AWS Management Console.
2. Монтируйте её на два EC2 инстанса с помощью команд NFS:
   ```bash
   sudo mount -t nfs4 <EFS-File-System-DNS>:<mount-point> /mnt/efs
   ```
3. Теперь оба инстанса могут работать с **общими файлами** как с локальной файловой системой.

## 4. **Когда использовать Amazon EFS?** 🔍

- **Совместное использование данных** между инстансами.
- **Веб-приложения** с несколькими EC2 инстансами, которым нужен доступ к одной и той же файловой системе.
- **Обработка больших данных** или логов, которые нужно анализировать несколькими инстансами одновременно.

## 5. **Резюме** 📝

- **Amazon EFS** позволяет создавать **общую файловую систему** между несколькими **EC2 инстансами**, обеспечивая **масштабируемость** и **высокую доступность**.
- Идеально подходит для сценариев, где требуется **совместное использование файлов**, например, в **веб-приложениях** или для **анализа данных**.

</p></details>

---

# 2. Linux

### ❓ 1. Что такое inode?

**[📺 Видео-вопрос (34:36)](https://youtu.be/r0OD5Ncvce0?t=2076)**

<details><summary><strong>Показать ответ</strong></summary><p>

### В Unix/Linux файловых системах `inode` (индексный дескриптор) — это структура данных, которая **хранит метаинформацию о файле**, **не включая его имя и содержимое**.

### 🧩 Что содержит inode?

| Поле                      | Описание                                            |
| ------------------------- | --------------------------------------------------- |
| Размер файла              | В байтах                                            |
| UID и GID                 | Владелец и группа                                   |
| Права доступа             | rwxr-xr-x и т.п.                                    |
| Время изменений           | `ctime` (мета), `mtime` (содерж.), `atime` (доступ) |
| Кол-во жёстких ссылок     | Сколько имён ссылаются на inode                     |
| Указатели на блоки данных | Где хранится содержимое файла                       |

### 🔗 Связь между именем и inode

- Имена файлов хранятся в **каталогах**, которые ссылаются на inode.
- Один inode может быть связан с несколькими именами (жёсткие ссылки).

```bash
ls -i file.txt
123456 file.txt
```

### 🔄 Что происходит при копировании и ссылках

| Операция             | Результат                            |
| -------------------- | ------------------------------------ |
| `cp file newfile`    | Создаётся новый inode                |
| `ln file hardlink`   | Новый файл ссылается на тот же inode |
| `ln -s file symlink` | Создаётся отдельный inode (с путём)  |

### 🛠 Полезные команды

### Посмотреть inode файла:

```bash
ls -i file.txt
```

### Найти файл по inode:

```bash
find . -inum 123456
```

### Показать использование inodes:

```bash
df -i
```

### Показать inodes всех файлов:

```bash
find . -printf "%i %p\n"
```

### ⚠️ Что если inodes закончились?

- Даже если есть свободное место (`df -h`), создание файлов может быть невозможно, если кончились `inodes`.
- Типичная причина: миллионы мелких файлов (например, в `/tmp`, `/var/log`, `/var/spool/...`).

Сообщение:

```
No space left on device
```

→ Но `df -i` покажет, что **inodes на 100%**.

### ✅ Вывод

- `inode` — это **основа хранения информации о файлах** в Linux
- Он **не содержит имени файла**, а только метаинформацию
- Мониторинг `inodes` важен для стабильной работы систем с большим числом мелких файлов

</p></details>

---

### ❓ 2. Чем отличается hard link от soft link?

**[📺 Видео-вопрос (35:11)](https://youtu.be/r0OD5Ncvce0?t=2111)**

<details><summary><strong>Показать ответ</strong></summary><p>

**Hard link** и **soft link** (или **symbolic link**) — это два различных способа создания ссылок на файлы в файловых системах. Оба типа ссылок используются для создания указателей на файлы, но работают они по-разному.

## 1. **Hard Link** 🧱

### Что это такое?

**Hard link** — это ссылка на **файл**, которая указывает на те же данные, что и оригинальный файл. В системе создаётся дополнительный указатель на **тот же блок данных**, и файл можно найти по нескольким разным именам.

### Основные характеристики:

- **Делит данные**: Hard link указывает на **одни и те же данные** на диске, что и оригинальный файл.
- **Один inode**: У обоих файлов (оригинального и hard link) будет одинаковый **inode**, так как они ссылаются на одни и те же блоки данных.
- **Независимость от исходного файла**: После создания hard link оригинальный файл и hard link **равнозначны**. Удаление оригинала не удаляет данные, так как hard link также указывает на эти данные.
- **Ограничения**:
  - Работает только внутри одной **файловой системы**.
  - Невозможно создать hard link для **директорий** (кроме специальных случаев, например, с правами суперпользователя).

### Пример:

```bash
# Создаём hard link для файла
ln original_file.txt hard_link.txt
```

## 2. **Soft Link (Symbolic Link)** 🪶

### Что это такое?

**Soft link** (или **symbolic link**) — это специальный файл, который содержит **путь** к оригинальному файлу. Это своего рода **ярлык**, который указывает на другой файл или директорию.

### Основные характеристики:

- **Ссылается на путь**: Soft link содержит текстовое представление пути к целевому файлу, а не ссылки на данные.
- **Разные inodes**: Soft link и целевой файл имеют **разные inodes**, так как они независимы друг от друга.
- **Может ссылаться на файлы или директории**: В отличие от hard link, soft link может ссылаться на **директории** и файлы, расположенные в разных файловых системах.
- **Зависимость от исходного файла**: Если оригинальный файл удалён или перемещён, soft link становится **неработающим** (broken link), так как путь больше не существует.

### Пример:

```bash
# Создаём soft link для файла
ln -s original_file.txt soft_link.txt
```

## 3. **Ключевые различия** ⚖️

| **Характеристика**       | **Hard Link**                                  | **Soft Link (Symbolic Link)**                               |
| ------------------------ | ---------------------------------------------- | ----------------------------------------------------------- |
| **Как работает**         | Указывает на те же данные (тот же inode).      | Указывает на путь к файлу (разные inodes).                  |
| **Удаление оригинала**   | Данные остаются доступными через hard link.    | После удаления оригинала soft link становится неработающим. |
| **Типы объектов**        | Работает только с файлами.                     | Может ссылаться на файлы и директории.                      |
| **Местоположение**       | Работает только внутри одной файловой системы. | Может ссылаться на файлы в разных файловых системах.        |
| **Символические ссылки** | Нет                                            | Да, всегда указывает на путь.                               |

## 4. **Когда использовать?** 🛠️

- **Hard Link**:

  - Когда нужно создать несколько имен для одного набора данных в пределах одной файловой системы.
  - Когда важно, чтобы удаление одного файла не влияло на доступность данных, так как hard link также ссылается на данные.

- **Soft Link**:
  - Когда необходимо создать ярлыки для файлов или директорий, особенно если они расположены в разных файловых системах.
  - Когда нужно создать ссылки на директории или использовать символические ссылки для удобства навигации.

## 5. **Резюме** 📝

- **Hard Link** указывает на те же данные и имеет тот же inode, что и оригинальный файл. Он остаётся рабочим даже после удаления оригинала.
- **Soft Link** (symbolic link) указывает на путь к файлу и имеет отдельный inode. Если файл удалён или перемещён, ссылку нельзя будет использовать.

</p></details>

---

### ❓ 3. В чем измеряется загрузка файловой системы?

**[📺 Видео-вопрос (38:05)](https://youtu.be/r0OD5Ncvce0?t=2284)**

<details><summary><strong>Показать ответ</strong></summary><p>

- Загрузка файловой системы в **Linux** может быть измерена различными способами, в том числе **занятостью пространства** в **кБ** (килобайтах) и **производительностью ввода-вывода (IOPS)** (операциями ввода-вывода в секунду). Рассмотрим, как это можно проверить.

## 1. **Занятость файловой системы (в кБ)** 🧮

Для того чтобы узнать, сколько пространства используется на файловой системе, можно использовать команду **`df`**. Она показывает использование пространства и доступность на всех смонтированных файловых системах.

### Пример:

```bash
df -h
```

- **`-h`** — показывает размеры в удобочитаемом формате (например, в **GB**, **MB**, **KB**).
- **Вывод команды**:
  - **Filesystem** — имя файловой системы.
  - **Size** — общий размер файловой системы.
  - **Used** — сколько пространства используется.
  - **Avail** — сколько пространства доступно.
  - **Use%** — процент использования.
  - **Mounted on** — точка монтирования файловой системы.

#### Пример вывода:

```bash
Filesystem     Size  Used Avail Use% Mounted on
/dev/sda1       50G   12G   38G  24% /
tmpfs           2G   1.2M   2G   1% /dev/shm
```

- В этом примере файловая система **`/dev/sda1`** имеет общий размер 50 GB, из которых 12 GB занято и 38 GB доступно.

## 2. **Производительность ввода-вывода (IOPS)** 💾

Для измерения производительности файловой системы по **IOPS** (операциям ввода-вывода в секунду), можно использовать утилиты **`iostat`** или **`iotop`**.

### Использование **`iostat`**:

Команда **`iostat`** показывает статистику ввода-вывода для устройств в реальном времени.

```bash
iostat -x 1
```

- **`-x`** — расширенный вывод, который включает дополнительные данные, такие как **IOPS** и **время ожидания операций**.
- **`1`** — интервал обновления в секундах (в данном случае 1 секунда).

### Столбцы вывода:

| **Параметр** | **Описание**                                                                                                          |
| ------------ | --------------------------------------------------------------------------------------------------------------------- |
| **Device**   | Имя устройства (например, **`sda`**, **`mmcblk0`** и т. д.).                                                          |
| **r/s**      | Количество операций чтения в секунду (IOPS для чтения).                                                               |
| **rkB/s**    | Количество данных, прочитанных в килобайтах в секунду (скорость чтения).                                              |
| **rrqm/s**   | Количество слияний операций чтения в секунду (Read Request Merges) — слияние нескольких запросов в один.              |
| **%rrqm**    | Процент слияний чтений от общего числа операций чтения.                                                               |
| **r_await**  | Среднее время ожидания операций чтения в миллисекундах.                                                               |
| **rareq-sz** | Средний размер операции чтения в килобайтах.                                                                          |
| **w/s**      | Количество операций записи в секунду (IOPS для записи).                                                               |
| **wkB/s**    | Количество данных, записанных в килобайтах в секунду (скорость записи).                                               |
| **wrqm/s**   | Количество слияний операций записи в секунду (Write Request Merges) — слияние нескольких запросов записи.             |
| **%wrqm**    | Процент слияний записей от общего числа операций записи.                                                              |
| **w_await**  | Среднее время ожидания операций записи в миллисекундах.                                                               |
| **wareq-sz** | Средний размер операции записи в килобайтах.                                                                          |
| **d/s**      | Количество операций удаления в секунду                                                                                |
| **dkB/s**    | Количество данных, удалённых в килобайтах в секунду                                                                   |
| **drqm/s**   | Количество слияний операций удаления в секунду.                                                                       |
| **%drqm**    | Процент слияний удалений от общего числа операций удаления.                                                           |
| **d_await**  | Среднее время ожидания операций удаления в миллисекундах.                                                             |
| **dareq-sz** | Средний размер операции удаления в килобайтах.                                                                        |
| **f/s**      | Количество операций файловых операций в секунду                                                                       |
| **f_await**  | Среднее время ожидания файловых операций в миллисекундах.                                                             |
| **aqu-sz**   | Средняя длина очереди ввода-вывода — показывает, сколько операций ввода-вывода ожидают своей обработки.               |
| **%util**    | Процент времени, когда устройство было занято обработкой операций ввода-вывода Это показатель нагрузки на устройство. |

### Примечания:

- **r/s** и **w/s**: Если эти значения равны **0.00**, это означает, что в данный момент на устройстве не выполняются операции чтения или записи.
- Если **%util** равно **0.00**, это означает, что устройство не используется для ввода-вывода и не обрабатывает запросы.

#### Резюме:

Вывод команды **`iostat -xd 1`** для устройства **mmcblk0** показывает, что на данный момент на устройстве **нет активности ввода-вывода**. Все параметры, связанные с операциями чтения, записи, удаления и файловыми операциями, равны **0.00**, что означает отсутствие операций ввода-вывода на устройстве в этот момент.

### Использование **`iotop`**:

**`iotop`** позволяет мониторить процессы, которые используют диск, с отображением **IOPS** (для чтения и записи).

```bash
sudo iotop
```

- Для запуска **`iotop`** требуются права администратора.
- Эта команда будет отображать в реальном времени статистику по процессам, использующим диск.

## 3. **Как проверить вывод?** 📊

- Для **IOPS**: смотрите на столбцы **`r/s`** и **`w/s`** в выводе **`iostat`**, это будет количество операций ввода-вывода в секунду.
- Для **занятости пространства**: используйте команду **`df -h`**, чтобы проверить, сколько пространства используется и сколько доступно.

### Пример:

```bash
df -h   # Проверка использования пространства
iostat -x 1   # Проверка IOPS и загруженности
```

</p></details>

---

### ❓ 4. Что такое LTS?

**[📺 Видео-вопрос (42:36)](https://youtu.be/r0OD5Ncvce0?t=2556)**

<details><summary><strong>Показать ответ</strong></summary><p>

- **LTS** (_Long-Term Support_) — это релиз программного обеспечения с **долгосрочной поддержкой**, которая включает:

- Обновления безопасности
- Исправления багов
- Поддержку критических компонентов

### 📊 Сравнение LTS и обычных релизов

| Критерий              | **LTS-релиз**                          | **Обычный релиз (non-LTS)**           |
| --------------------- | -------------------------------------- | ------------------------------------- |
| **Срок поддержки**    | 3–10 лет                               | 6–12 месяцев                          |
| **Стабильность**      | ✅ Максимально стабильный              | ❗ Возможны изменения, нестабильность |
| **Новые функции**     | ❌ Минимум новшеств, только стабильное | ✅ Новые и экспериментальные фичи     |
| **Частота релизов**   | Реже (раз в 2–3 года)                  | Чаще (раз в 6–12 месяцев)             |
| **Целевая аудитория** | Бизнес, продакшн, серверы              | Разработчики, тестовые окружения      |

### ✅ Когда использовать LTS?

- Для **продакшн-серверов**
- Когда важна **долговечность и безопасность**
- В критичных бизнес-приложениях
- Если не хочется часто обновлять платформу

### ⚠️ Когда подходит обычный релиз?

- Для **разработки и тестирования**
- Чтобы быстро получить **новые функции**
- В краткоживущих проектах

### 💡 Вывод

> **LTS** — это надёжность, стабильность и поддержка  
> **Non-LTS** — это свежесть, гибкость и новизна

Выбор зависит от целей: **продакшн → LTS**, **разработка → non-LTS**

</p></details>

---

### ❓ 5. Как оценить загрузку процессора?

**[📺 Видео-вопрос (44:20)](https://youtu.be/r0OD5Ncvce0?t=2660)**

<details><summary><strong>Показать ответ</strong></summary><p>

- С помощью **`top`** можно отслеживать загрузку процессора, использование памяти, состояние процессов и другие системные ресурсы.

## 1. **Запуск команды top** 🚀

Для запуска **`top`** откройте терминал и введите команду:

```bash
top
```

Эта команда выведет список процессов, их использование ресурсов и общую информацию о системе.

## 2. **Интерпретация вывода команды top** 📊

При запуске команды **`top`** выводится несколько строк статистики, включая информацию о процессоре. Основные поля, относящиеся к загрузке процессора:

```
top - 14:22:15 up  1:23,  2 users,  load average: 0.10, 0.20, 0.30
Tasks: 149 total,   1 running, 148 sleeping,   0 stopped,   0 zombie
%Cpu(s):  2.1 us,  1.2 sy,  0.0 ni, 96.7 id,  0.0 wa,  0.0 hi,  0.0 si,  0.0 st
```

### Объяснение строк:

1. **Загрузка системы (load average)**:

   - **`load average: 0.10, 0.20, 0.30`** — это показатели средней загрузки системы за последние 1, 5 и 15 минут. Эти значения показывают среднюю нагрузку на процессор. Если они превышают количество доступных ядер процессора, это может свидетельствовать о том, что система перегружена.

2. **Статистика процессора**:

   ```
   %Cpu(s):  2.1 us,  1.2 sy,  0.0 ni, 96.7 id,  0.0 wa,  0.0 hi,  0.0 si,  0.0 st
   ```

   Это строка отображает различные статистические показатели использования процессора. Вот что означают все значения:

   - **`us` (user)**: Процент времени процессора, который тратится на выполнение пользовательских процессов (не относящихся к ядру). В данном примере **2.1%** процессора используется на выполнение пользовательских процессов.
   - **`sy` (system)**: Процент времени процессора, который тратится на выполнение процессов ядра (системных процессов). Здесь **1.2%** процессора используется для выполнения системных задач.
   - **`ni` (nice)**: Процент времени, которое процессор тратит на выполнение процессов с изменённым приоритетом (с модификатором **nice**). В данном случае **0.0%**.
   - **`id` (idle)**: Процент времени, когда процессор простаивает (не занят обработкой задач). Это время, когда процессор не занят никакими активными процессами. В примере **96.7%** процессора не используется (большая часть времени в режиме покоя).
   - **`wa` (iowait)**: Процент времени, которое процессор тратит на ожидание завершения операций ввода/вывода (например, чтение с диска). Здесь **0.0%**.
   - **`hi` (hardware interrupts)**: Процент времени, затрачиваемого процессором на обработку аппаратных прерываний. В данном случае **0.0%**.
   - **`si` (software interrupts)**: Процент времени, затрачиваемого процессором на обработку программных прерываний. В данном случае **0.0%**.
   - **`st` (steal time)**: Процент времени, когда процессор используется виртуальными машинами, которые находятся на том же хосте. **0.0%** в этом примере.

### Пояснение:

- **`us` + `sy` + `ni`** = время процессора, которое используется для выполнения задач (пользовательские процессы + системные задачи + приоритетные процессы).
- **`id`** = время бездействия процессора (свободное время).
- **`wa`** = время ожидания операций ввода-вывода.
- **`hi`** + **`si`** = время, которое процессор тратит на обработку прерываний (аппаратных и программных).
- **`st`** = время, которое процессор тратит на работу виртуальных машин.

## 3. **Как использовать топ для оценки загрузки процессора** 🧑‍💻

- Когда вы смотрите на статистику в **`top`**, основное внимание стоит уделить:

  - **`%Cpu(s)`**, чтобы оценить текущую загрузку процессора.
  - **`load average`**, чтобы проверить, насколько сильно система загружена за последние 1, 5 и 15 минут.

- Например:
  - Если **`%Cpu(s)`** показывает, что **`id` (бездеятельность процессора)** высокое (например, 90% и выше), это значит, что процессор не загружен.
  - Если **`load average`** превышает количество ядер процессора, это может указывать на перегрузку системы.

## 4. **Дополнительные полезные опции для команды `top`**:

- **`top -i`** — отобразить только активные процессы.
- **`top -u <user>`** — отобразить процессы, принадлежащие конкретному пользователю.
- **`top -p <pid>`** — мониторить только конкретный процесс по его PID.

</p></details>

---

# 3. Docker

### ❓ 1. В чем разница между ENTRYPOINT и CMD в Dockerfile?

**[📺 Видео-вопрос (47:10)](https://youtu.be/r0OD5Ncvce0?t=2830)**

<details><summary><strong>Показать ответ</strong></summary><p>

### Оба используются для задания команды, которую выполняет контейнер при запуске, но у них разные цели и поведение.

### 📦 CMD

- Задаёт **команду или аргументы по умолчанию**
- Легко **переопределяется** при запуске контейнера

```Dockerfile
CMD ["nginx", "-g", "daemon off;"]
```

```bash
docker run myimage echo "Hello"
```

🔹 Контейнер выполнит: `echo Hello`, а не `nginx`

### 🚀 ENTRYPOINT

- Задаёт **основную команду**, которую контейнер всегда выполняет
- Аргументы можно передать, но сама команда остаётся

```Dockerfile
ENTRYPOINT ["nginx"]
```

```bash
docker run myimage -g "daemon off;"
```

🔹 Контейнер выполнит: `nginx -g "daemon off;"`

### 🔗 Совместное использование

Можно использовать `ENTRYPOINT` для команды и `CMD` для аргументов по умолчанию:

```Dockerfile
ENTRYPOINT ["nginx"]
CMD ["-g", "daemon off;"]
```

🔹 По умолчанию: `nginx -g "daemon off;"`  
🔹 Можно переопределить только `CMD` при запуске.

### 📊 Сравнительная таблица

| Характеристика         | ENTRYPOINT                     | CMD                                |
| ---------------------- | ------------------------------ | ---------------------------------- |
| Цель                   | Основная неизменяемая команда  | Аргументы или команда по умолчанию |
| Переопределяется?      | ❌ Только через `--entrypoint` | ✅ Через аргументы `docker run`    |
| Форматы                | `exec` и `shell`               | `exec` и `shell`                   |
| Часто используется для | Агентов, скриптов, CLI-утилит  | Простых конфигураций, тестов       |

### 📘 Заключение

- Используй `ENTRYPOINT`, если контейнер всегда должен выполнять конкретную программу
- Используй `CMD` для аргументов или как запасной вариант, который можно легко заменить
- В паре они дают наибольшую гибкость и контроль

</p></details>

---

### ❓ 2. От имени какого пользователя будет запущена программа, указанная в командных инструкциях CMD или ENTRYPOINT в Docker контейнере?

**[📺 Видео-вопрос (48:13)](https://youtu.be/r0OD5Ncvce0?t=2893)**

<details><summary><strong>Показать ответ</strong></summary><p>

- По умолчанию, программа, указанная в командных инструкциях **`CMD`** или **`ENTRYPOINT`** в **Docker контейнере**, будет запущена от имени пользователя **`root`**, если в Dockerfile явно не указано другое имя пользователя.

## 1. **Как изменить пользователя?** 🛠️

Если вы хотите, чтобы программа запускалась от имени другого пользователя, можно использовать инструкцию **`USER`** в Dockerfile перед **`CMD`** или **`ENTRYPOINT`**.

### Пример:

```dockerfile
FROM ubuntu

# Создаем нового пользователя
RUN useradd -ms /bin/bash myuser

# Устанавливаем пользователя, от имени которого будет запускаться программа
USER myuser

# Указываем команду для запуска
CMD ["echo", "Hello from myuser!"]
```

В этом примере программа будет выполняться от имени **`myuser`**, а не `root`.

## 2. **Что происходит по умолчанию?** ⚙️

- Если **`USER`** не задан в Dockerfile, контейнер будет работать от имени **`root`**.
- Если указано несколько инструкций **`USER`** в Dockerfile, то будет использоваться последний указанный пользователь.

### Пример:

```dockerfile
FROM ubuntu

# Создаем пользователя
RUN useradd -ms /bin/bash user1

# Программа будет запускаться от имени user1
USER user1

# Команда для запуска
CMD ["echo", "Hello from user1!"]

# Создаем еще одного пользователя
RUN useradd -ms /bin/bash user2

# Программа будет запускаться от имени user2
USER user2

CMD ["echo", "Hello from user2!"]
```

В данном случае программа будет запускаться от имени **`user2`**, так как последний **`USER`** указан как **`user2`**.

## 3. **Резюме** 📝

- По умолчанию, программа в Docker контейнере запускается от имени **`root`**.
- Чтобы изменить пользователя, используйте инструкцию **`USER`** в Dockerfile.
- Если указаны несколько пользователей, программа будет запускаться от имени последнего указанного пользователя.

</p></details>

---

### ❓ 3. Будет ли контейнер запущен автоматически после перезагрузки хоста?

**[📺 Видео-вопрос (50:35)](https://youtu.be/r0OD5Ncvce0?t=3035)**

<details><summary><strong>Показать ответ</strong></summary><p>

- Контейнер может быть настроен для автоматического запуска после перезагрузки хоста, в зависимости от конфигурации и системы управления контейнерами. В этом руководстве описаны основные способы настройки автозапуска для контейнеров с использованием **Docker**, **Docker Compose**, **Systemd** и **Kubernetes**.

## 1. **Docker** 🐳

Для **Docker** используется параметр `--restart`, который определяет, будет ли контейнер автоматически перезапущен после перезагрузки хоста или сбоя контейнера.

### Возможные опции для параметра `--restart`:

- **no**: Контейнер не будет автоматически перезапущен (по умолчанию).
- **always**: Контейнер всегда будет перезапущен, даже если завершится нормально.
- **unless-stopped**: Контейнер будет перезапущен, если он не был остановлен вручную.
- **on-failure**: Контейнер будет перезапущен только в случае сбоя.

### Пример:

```bash
docker run --restart always my_container
```

## 2. **Docker Compose** 📦

Если вы используете **Docker Compose**, настройка автозапуска контейнера также возможна с помощью параметра `restart` в файле `docker-compose.yml`.

### Пример:

```yaml
version: '3'
services:
  my_service:
    image: my_image
    restart: always
```

</p></details>

---

### ❓ 4. Что такое multistage build в Docker?

**[📺 Видео-вопрос (51:42)](https://youtu.be/r0OD5Ncvce0?t=3102)**

<details><summary><strong>Показать ответ</strong></summary><p>

- **Мультистейдж билд** в Docker — это техника, которая позволяет оптимизировать процесс сборки Docker образа, используя несколько этапов (стадий) в одном Dockerfile. Это позволяет снизить размер конечного образа, улучшив производительность и избавившись от лишних зависимостей.

## 🏗️ **Как работает мультистейдж билд?**

1. **Несколько стадий сборки:** Каждый этап сборки определен с помощью отдельной директивы **`FROM`**, где можно использовать разные образы для разных стадий (например, для сборки приложения, тестирования и т. д.).
2. **Передача артефактов:** На каждом этапе могут быть использованы только те данные, которые необходимы для следующего этапа. Лишние зависимости или инструменты, установленные для сборки, можно исключить в финальном образе.
3. **Оптимизация размера:** За счет удаления ненужных файлов, зависимостей и инструментов на поздних стадиях, конечный образ будет меньше.

## 📦 **Пример использования мультистейдж билда:**

```dockerfile
# Этап 1: Сборка
FROM golang:1.16 AS builder

WORKDIR /app
COPY . .
RUN go build -o myapp .

# Этап 2: Финальный образ
FROM alpine:latest

WORKDIR /root/
COPY --from=builder /app/myapp .

CMD ["./myapp"]
```

### 🔍 **Объяснение:**

1. **Первый этап (builder):**

   - Используется образ **`golang:1.16`** для сборки приложения.
   - Все файлы копируются в контейнер, и выполняется команда **`go build`** для создания исполняемого файла.
   - Этот этап называется `builder`, и он содержит все зависимости для сборки.

2. **Второй этап (финальный):**
   - Используется минималистичный образ **`alpine:latest`**, который не содержит всех инструментов для сборки.
   - В этот образ копируется только нужный исполняемый файл **`myapp`** из предыдущего этапа, с помощью команды **`COPY --from=builder`**.
   - В финальном образе остаются только необходимые артефакты.

## 🛠️ **Преимущества мультистейдж билда:**

- **Снижение размера образа:** Из образа исключаются ненужные инструменты и зависимости, оставляя только то, что необходимо для работы приложения.
- **Упрощение Dockerfile:** Вы можете разделить процесс на несколько этапов, каждый из которых отвечает за свою задачу.
- **Безопасность:** Избегание размещения инструментария для сборки (например, компиляторов или пакетов разработки) в финальном образе, что может снизить риски для безопасности.

## 🎯 **Итог:**

Мультистейдж билды позволяют создавать более эффективные, компактные и безопасные Docker образы, разделяя процесс сборки на несколько стадий и исключая лишние зависимости из финального контейнера.

</p></details>

---

### ❓ 5. Для чего в Dockerfile может быть несколько инструкций FROM и как это работает?

**[📺 Видео-вопрос (53:19)](https://youtu.be/r0OD5Ncvce0?t=3199)**

<details><summary><strong>Показать ответ</strong></summary><p>

В **Dockerfile** может быть несколько инструкций **`FROM`** для реализации **многоконтейнерных сборок** (Multistage Builds). Это позволяет разделить процесс сборки на несколько этапов, каждый из которых использует свой базовый образ. Такой подход помогает создавать **оптимизированные** и **легкие** образы.

## 1. **Как работает несколько инструкций `FROM`?** 🛠️

Каждая инструкция **`FROM`** в **Dockerfile** создает новый этап сборки. Каждый этап может использовать свой собственный базовый образ и выполнять определенные задачи, такие как установка зависимостей, сборка приложения или создание финального образа.

После выполнения всех этапов, в финальный образ можно добавить только необходимые файлы и исключить все промежуточные артефакты и инструменты сборки.

### Пример многоконтейнерной сборки:

```dockerfile
# Этап 1: Сборка приложения
FROM node:14 AS builder
WORKDIR /app
COPY . .
RUN npm install
RUN npm run build

# Этап 2: Финальный образ
FROM nginx:alpine
COPY --from=builder /app/build /usr/share/nginx/html
```

### Пояснение:

1. **Первый этап**:
   - Используется образ **`node:14`** для установки зависимостей и сборки приложения.
   - Этап помечен как **`AS builder`**, что позволяет использовать его в последующих этапах.
2. **Второй этап**:
   - Используется легкий образ **`nginx:alpine`**, предназначенный для размещения статического контента.
   - С помощью команды **`COPY --from=builder`** копируются собранные файлы из первого этапа, исключая все ненужные файлы сборки.

## 2. **Преимущества использования нескольких инструкций `FROM`** 🌟

### 1. **Меньший размер финального образа** 💾

Вы можете исключить инструменты сборки и другие ненужные файлы, что позволяет значительно уменьшить размер итогового образа.

### 2. **Управляемость и гибкость** 🔧

Каждый этап сборки может использовать свой собственный образ, что позволяет структурировать процесс сборки более гибко.

### 3. **Безопасность** 🔒

С помощью многоконтейнерных сборок можно создать более безопасный образ, используя минимальные образы для финальных стадий (например, для веб-серверов), что исключает лишние зависимости.

### 4. **Производительность** 🚀

Многоконтейнерные сборки делают процесс сборки более быстрым и эффективным, так как можно использовать легкие образы на финальных стадиях.

## 3. **Когда использовать многоконтейнерные сборки?** 🗂️

Многоконтейнерные сборки полезны в следующих случаях:

- Когда нужно собрать приложение в одном контейнере, а в другом использовать только его финальные артефакты.
- Когда нужно уменьшить размер образа, исключив все лишние зависимости и временные файлы.
- Когда важно улучшить безопасность, используя минимальные образы на финальных этапах.

## 4. **Резюме** 📝

Использование нескольких инструкций **`FROM`** в **Dockerfile** позволяет разделить процесс сборки на несколько этапов и улучшить оптимизацию Docker образов. Это уменьшает размер итогового образа, улучшает безопасность и делает процесс сборки более гибким и эффективным.

С помощью **многоконтейнерных сборок** вы можете:

- Собирать приложение в одном контейнере, а затем перенести только необходимые файлы в финальный контейнер.
- Сократить размер образов и улучшить их безопасность.
- Упростить структуру Dockerfile и улучшить производительность.

</p></details>

---

### ❓ 6. Например, если внутри контейнера работает демон, который слушает на порту 80, будет ли он автоматически доступен на хосте?

**[📺 Видео-вопрос (54:39)](https://youtu.be/r0OD5Ncvce0?t=3279)**

<details><summary><strong>Показать ответ</strong></summary><p>

- Если внутри Docker контейнера работает демон, который слушает на порту (например, порт 80), он **не будет автоматически доступен на хосте**. Для того чтобы контейнер мог взаимодействовать с хостом или другими контейнерами, нужно настроить **проброс портов**.

## 1. **Как работает проброс портов?** 🔄

1. **Контейнер**: Контейнер может слушать на определенном порту (например, порт 80 внутри контейнера).
2. **Проброс портов**: Чтобы этот порт был доступен извне (например, с хоста или сети), нужно указать проброс порта при запуске контейнера с помощью флага **`-p`** или **`--publish`**.

### Пример проброса порта:

Чтобы пробросить порт 80 контейнера на порт 80 хоста, используйте команду:

```bash
docker run -p 80:80 my_image
```

- **`80:80`** — это проброс порта, где:
  - Первая часть **`80`** — это порт на **хосте**.
  - Вторая часть **`80`** — это порт внутри **контейнера**.
- Это означает, что внешний порт 80 будет связан с портом 80 внутри контейнера, и теперь он будет доступен на хосте.

## 2. **Как это работает?** ⚙️

- **Без проброса портов**: Если проброс портов не настроен, контейнер может слушать на порту (например, порт 80), но этот порт будет доступен только внутри контейнера и не будет виден с хоста.
- **С пробросом портов**: После настройки проброса порта, вы сможете подключиться к сервису внутри контейнера через порт хоста (например, порт 80).

## 3. **Пример проброса нескольких портов** 🔢

Если вы хотите пробросить несколько портов, используйте такую команду:

```bash
docker run -p 8080:80 -p 443:443 my_image
```

- **Порт 80** внутри контейнера пробрасывается на **порт 8080** хоста.
- **Порт 443** внутри контейнера пробрасывается на **порт 443** хоста.

## 4. **Резюме** 📝

- Чтобы порт, на котором работает демон в Docker контейнере (например, порт 80), был доступен на хосте, необходимо использовать опцию **`-p`** для проброса порта при запуске контейнера.
- Без проброса портов сервис внутри контейнера будет недоступен с хоста.

</p></details>

---

### ❓ 7. Как привязать контейнер к сетевому интерфейсу хоста?

**[📺 Видео-вопрос (55:05)](https://youtu.be/r0OD5Ncvce0?t=3305)**

<details><summary><strong>Показать ответ</strong></summary><p>

- Чтобы привязать контейнер к определенному сетевому устройству хоста в Docker, можно использовать несколько методов в зависимости от требуемой изоляции сети и уровня доступа. Ниже приведены основные способы привязки контейнера к сетевому интерфейсу хоста.

## 1. **Использование параметра `--network host`** 🖥️

Этот метод позволяет контейнеру использовать **сетевой стек хоста**. Контейнер будет использовать тот же IP-адрес и интерфейсы, что и хост, и все порты контейнера будут доступны через порты хоста.

### Пример:

```bash
docker run --network host my_container
```

### Особенности:

- Контейнер будет **слушать порты хоста**, доступность которых можно проверить через стандартные команды, такие как `netstat`.
- **Не поддерживается в Windows**, доступно только на **Linux**.
- **Не изолирует контейнер от сети хоста**.

## 2. **Использование сети `bridge`** 🌉

Сеть **bridge** — это стандартная сеть Docker, которая использует виртуальный мост для связи между контейнерами и хостом. Контейнеры могут быть подключены к этой сети и взаимодействовать с хостом через неё.

### Пример:

1. Создание новой bridge-сети:

   ```bash
   docker network create      --driver=bridge      --subnet=192.168.1.0/24      --gateway=192.168.1.1      my_bridge_network
   ```

2. Запуск контейнера с использованием этой сети:
   ```bash
   docker run --network my_bridge_network my_container
   ```

### Особенности:

- **Изоляция** между контейнерами и хостом.
- Используется для **стандартных приложений** без привязки к физическому интерфейсу хоста.

## 3. **Использование параметра `--net` для указания интерфейса** 🌍

Можно указать контейнеру использовать конкретный интерфейс хоста через параметр `--net`, чтобы привязать контейнер к желаемому интерфейсу.

### Пример:

```bash
docker run --net host --name my_container my_image
```

### Особенности:

- Контейнер будет использовать интерфейс хоста для связи с внешней сетью.
- Простой способ для приложений, которые требуют доступ к **физическим интерфейсам хоста**.

## 4. **Использование сети `macvlan`** 🌐

Если необходимо, чтобы контейнер имел **собственный MAC-адрес** и был доступен напрямую через физическую сеть хоста, можно использовать **macvlan**.

### Пример:

1. Создание сети `macvlan`:

   ```bash
   docker network create      -d macvlan      --subnet=192.168.1.0/24      --gateway=192.168.1.1      -o parent=eth0 my_macvlan_network
   ```

2. Запуск контейнера:
   ```bash
   docker run --net my_macvlan_network --name my_container my_image
   ```

### Особенности:

- Контейнер **получает уникальный IP-адрес** в сети.
- Контейнер доступен в **физической сети**, как отдельное устройство.

## 5. **Использование параметра `--device` для доступа к физическим устройствам** 🖧

Если контейнеру нужно получить доступ к физическому сетевому устройству хоста (например, к интерфейсу туннеля или виртуальной сети), можно использовать параметр `--device`.

### Пример:

```bash
docker run --device /dev/net/tun my_container
```

### Особенности:

- Контейнер может работать с **физическими устройствами** хоста.
- Полезно для приложений, которые взаимодействуют с **виртуальными сетевыми интерфейсами**.

## 📋 Заключение

В зависимости от ваших требований к изоляции сети и доступа к устройствам хоста, Docker предоставляет несколько методов привязки контейнера к сетевому интерфейсу хоста:

- **`--network host`** для полного доступа к сети хоста.
- **Bridge-сеть** для изоляции контейнеров и хоста.
- **`macvlan`** для получения уникального IP-адреса контейнера в физической сети.
- **`--device`** для доступа к физическим сетевым устройствам.

</p></details>

---

### ❓ 8. Для чего используется инструкция EXPOSE в Dockerfile?

**[📺 Видео-вопрос (55:06)](https://youtu.be/r0OD5Ncvce0?t=3366)**

<details><summary><strong>Показать ответ</strong></summary><p>

- Инструкция **`EXPOSE`** в **Dockerfile** используется для **документирования**, какие порты контейнер должен прослушивать во время его работы. Это помогает другим пользователям и инструментам понять, какие порты будет использовать контейнер, но **не пробрасывает** порты на хост.

## 1. **Что делает инструкция EXPOSE?** 📢

Инструкция **`EXPOSE`** указывает, что контейнер должен слушать определенный порт, но не пробрасывает его на хост. Для того чтобы порт стал доступен на хосте, необходимо использовать флаг **`-p`** при запуске контейнера.

### Пример использования:

```dockerfile
# Базовый образ
FROM nginx:alpine

# Указываем, что контейнер будет слушать порт 80
EXPOSE 80

# Указываем команду для запуска
CMD ["nginx", "-g", "daemon off;"]
```

В этом примере контейнер будет слушать порт **80**, но этот порт не будет доступен на хосте, пока не будет настроен проброс порта.

## 2. **Как использовать EXPOSE?** 🛠️

- Инструкция **`EXPOSE`** указывает, какие порты контейнер будет слушать во время работы.
- Однако она **не пробрасывает порты** на хост, для этого необходимо использовать флаг **`-p`** при запуске контейнера.

### Пример команды для проброса порта:

```bash
docker run -p 80:80 my_nginx_image
```

Этот флаг пробрасывает порт **80** внутри контейнера на порт **80** хоста, что делает контейнер доступным через этот порт.

## 3. **Резюме** 📝

- **`EXPOSE`** служит для **документации** и информирует, какие порты контейнер будет использовать. Это полезно для других разработчиков, чтобы они знали, на каких портах ожидается слушать сервис.
- Чтобы сделать порт доступным на хосте, нужно использовать команду **`docker run -p`**, которая пробрасывает порт контейнера на порт хоста.

</p></details>

---
